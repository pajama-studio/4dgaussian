import { B, S, Q } from './shared.mjs';
export const learning = [
{
 id:'05-direction-becomes-color',title:B('One direction, three independent color sums','一个方向，三组独立的颜色求和'),phase:1,lab:'sh',
 goal:B('Read SH as weighted directional patterns and explain why separate channels produce a colored sphere.','把 SH 看成方向图案的加权组合，解释独立通道为什么能组成彩色球。'),
 hook:B('A shiny surface looks different as you move around it. A single RGB triple cannot express that change. Instead, store a small recipe that takes a viewing direction and returns RGB.','你绕着有光泽的表面移动时，看到的颜色会改变。单个 RGB 三元组无法表达这种变化，于是存一份小配方：输入观察方向，输出 RGB。'),
 steps:[
 S('Use a direction, not a position','先得到方向，而不是位置','Subtract camera position from the Gaussian center and divide by the vector length. This produces a unit vector v: three numbers whose squared values sum to one. Our SH convention uses camera → Gaussian. Reversing it flips odd-degree SH terms, so sign conventions must match the exporter.','用 Gaussian 中心减相机位置，再除以向量长度，得到单位方向 v：三个分量平方和等于 1。本教学采用相机 → Gaussian 的方向。方向取反会翻转奇数阶 SH 项，因此必须与导出端保持一致。',String.raw`\mathbf v=\frac{\boldsymbol\mu-\mathbf c}{\|\boldsymbol\mu-\mathbf c\|},\qquad v_x^2+v_y^2+v_z^2=1`),
 S('Build with reusable patterns','用固定的方向图案搭颜色','Spherical harmonics are fixed functions on the unit sphere. Degree zero is constant. Degree one supplies patterns proportional to −y, z and −x in our convention. Higher degrees provide finer angular variation. A coefficient says how strongly a pattern contributes; the pattern itself does not change for each Gaussian.','球谐函数是在单位球面上的一组固定函数。零阶是常数；在本约定中，一阶分别与 −y、z、−x 成比例。更高阶表达更细的角度变化。系数决定某个图案占多大比重，基函数本身不会因 Gaussian 不同而改变。',String.raw`Y_0=\frac1{\sqrt{4\pi}},\quad(Y_1,Y_2,Y_3)=\sqrt{\frac3{4\pi}}(-v_y,v_z,-v_x)`),
 S('Repeat the sum independently for R, G and B','R、G、B 分别做一次求和','For each direction, evaluate the basis once. Multiply those same basis values by three separate coefficient lists, then sum each channel. Up to degree L there are (L + 1)² basis functions. Degree three therefore needs 16 coefficients per channel, 48 scalar coefficients in total. Sharing basis values does not mix channels.','每个方向只需算一次基函数，再分别乘 R、G、B 三列系数，各自求和。到 L 阶共有 (L + 1)² 个基函数。三阶就是每通道 16 个系数，总计 48 个标量。共用基函数并不意味着混合通道。',String.raw`c_h(\mathbf v)=\frac12+\sum_{k=0}^{(L+1)^2-1}a_{kh}Y_k(\mathbf v),\qquad h\in\{R,G,B\}`),
 S('Interpret the colored sphere correctly','正确理解彩色球','Each point on the visualization sphere represents a direction, not a point on a physical Gaussian surface. At that direction, three scalar results become one displayed RGB color. The red-only, green-only and blue-only views expose the separate sums. Plot rotation changes how you inspect the function; it must not silently change its coefficients.','可视化球面上的每个位置代表一个方向，不代表真实 Gaussian 表面的位置。在这个方向算出的三个标量合为一个显示 RGB。R、G、B 单独视图展示各自的和。旋转图示相机只是换角度查看函数，不应偷偷修改系数。'),
 S('Match the actual appearance contract','核对真实模型的外观契约','The +0.5 offset is an implementation convention. Our teaching evaluator follows Graphdeco’s nonnegative output clamp and then separately clips to 0–1 for display. The STG-Lite fixture in our viewer stores direct RGB, not learned higher-order SH. Full STG feature rendering and this SH lesson are different appearance models.','+0.5 是实现约定。教学求值器先按 Graphdeco 方式把负输出截到零，再单独截到 0–1 用于显示。当前 STG-Lite 真实模型存的是直接 RGB，不是学到的高阶 SH。完整 STG 的特征渲染与本课 SH 是不同外观模型。'),
 ],
 worked:B('Take baseline RGB = (0.5, 0.5, 0.5) and only a₂R = 0.4. At +Z, Y₂ ≈ 0.4886, so RGB ≈ (0.6954, 0.5, 0.5). At −Z, it is (0.3046, 0.5, 0.5). Green and blue never change.','基准 RGB = (0.5, 0.5, 0.5)，只令 a₂R = 0.4。在 +Z，Y₂ ≈ 0.4886，RGB ≈ (0.6954, 0.5, 0.5)；在 −Z 则为 (0.3046, 0.5, 0.5)。绿色与蓝色始终没变。'),
 pitfall:B('An SH lobe’s gold/blue colors mean positive/negative function values. They are not the red/green/blue appearance channels.','SH 花瓣的金色/蓝色表示函数值的正负，不是外观的红绿蓝通道。'),
 task:B('Move polar angle from 0° to 180°. Change only the red coefficient, then inspect all three scalar outputs. Open the full 3D lab to rotate the channel spheres.','将极角从 0° 调到 180°，只修改红色系数，观察三个标量输出，再打开完整三维实验旋转各通道球面。'),
 code:[['sh-basis-full',B('These are the actual 16 basis polynomials used by the SH lab.','这是 SH 实验实际使用的 16 个基函数多项式。')],['sh-color-full',B('Each RGB component accumulates its own coefficient products.','每个 RGB 分量分别累加自己的系数乘积。')]],refs:['gs','stg'],math:'sh-lab',
 quizzes:[Q('Changing only a red SH coefficient directly changes…','只修改一个红通道 SH 系数，会直接改变……',[B('Only the red sum','仅红色求和'),B('All three sums','三个通道的和'),B('The Gaussian geometry','Gaussian 的几何')],0,[B('Correct. The same basis value is multiplied by independent coefficients.','正确，相同基函数值乘的是各通道独立系数。'),B('Basis values are shared, coefficients are not.','共用的是基函数值，不是系数。'),B('Appearance coefficients do not change covariance or position.','外观系数不会直接改变协方差或位置。')])],
},
{
 id:'06-add-time',title:B('What the fourth dimension actually changes','第四个维度到底改变了什么'),phase:1,lab:'time',
 goal:B('Evaluate an STG-Lite record at a timestamp and distinguish it from deformation and joint 4D Gaussians.','在某个时间求值 STG-Lite，并分清形变与联合四维高斯。'),
 hook:B('A moving point does not need a new file for every video frame. Its record can contain a short motion recipe plus a time window in which it matters.','一个运动点不一定每个视频帧都要存一个新文件。它的记录可以包含一份简短的运动配方，以及它发挥作用的时间窗口。'),
 steps:[
 S('Agree on the clock first','先统一时钟','Normalize capture time with a documented start and duration. A file using t in 0–1 cannot be evaluated with seconds directly. Playback speed changes the rate at which t advances, not the learned motion coefficients. Our browser fixture maps its capture interval to the normalized model time.','用明确记录的起点与时长归一化时间。模型若使用 0–1 的 t，就不能直接传入秒数。播放速度改变的是 t 推进的速度，不是学到的运动系数。浏览器真实模型将采集区间映射到模型的归一化时间。',String.raw`t=\frac{t_{\rm seconds}-t_{\rm start}}{D},\qquad\Delta t=t-\tau`),
 S('Position is a short polynomial','位置是一段短多项式','Start at μ₀ when t = τ. Add a term proportional to elapsed time, another to its square and another to its cube. Each coefficient is a 3D vector. The square and cube allow bending and changing speed. These are fitted coefficients; calling m₂ acceleration is inaccurate unless you include the derivative’s factor of two.','t = τ 时从 μ₀ 出发，再加与时间差、时间差平方、时间差立方成比例的三项。每个系数都是三维向量，平方与立方使轨迹能弯、速度能变。它们是拟合系数；若把 m₂ 直接叫加速度，就漏掉求导后的系数 2。',String.raw`\boldsymbol\mu(t)=\boldsymbol\mu_0+\mathbf m_1\Delta t+\mathbf m_2\Delta t^2+\mathbf m_3\Delta t^3`),
 S('Being present is separate from moving','是否出现与怎么移动是两件事','Multiply base opacity by a time window centered at τ. For this STG convention the exponent has no one-half factor. At one width ρ from the center, the multiplier is e⁻¹ ≈ 0.368. A narrow window makes a primitive fade away outside its useful time interval; it does not stop its polynomial from being evaluable.','基准 opacity 再乘以 τ 为中心的时间窗。本 STG 约定的指数前没有二分之一。离中心一倍时间宽度 ρ 时，倍率为 e⁻¹ ≈ 0.368。窄窗口使点在有效时间外淡出，但不意味着它的运动多项式无法求值。',String.raw`o(t)=\mathrm{sigmoid}(\beta)\,e^{-((t-\tau)/\rho)^2},\qquad\rho=e^\ell`),
 S('Orientation changes before projection','旋转也要在投影前求出','In the current record format, form q₀ + Δtω and normalize this four-component vector to get orientation. It is not a general physical angular-velocity integrator. Use the resulting rotation and scales to build covariance, then run the same projection and compositing pipeline as in earlier lessons.','在当前格式中，先算 q₀ + Δtω，再把这个四分量向量归一化得到方向。它不是通用物理角速度积分器。用得到的旋转与尺度构造协方差，再走前面相同的投影和合成流程。',String.raw`q(t)=\frac{q_0+\Delta t\,\omega}{\|q_0+\Delta t\,\omega\|}`),
 S('Other 4D methods put time elsewhere','其他 4D 方法把时间放在别处','A deformation model predicts changes to canonical 3D Gaussians using a network and time-dependent features. A joint 4D Gaussian stores a spacetime covariance and conditions on t to obtain a 3D slice. In that second case, spatial–temporal covariance controls the conditional center shift. These models share a rendering destination but not a parameter layout or decoder.','形变方法用网络与随时间变化的特征，预测标准三维 Gaussian 的变化。联合四维高斯存时空协方差，在给定 t 后条件化得到三维切片；空间与时间的交叉协方差控制条件中心的偏移。它们最后都可走三维渲染，但参数布局和解码器不同。',String.raw`\mu_{x\mid t}=\mu_x+\Sigma_{xt}\Sigma_{tt}^{-1}(t-\mu_t),\quad\Sigma_{x\mid t}=\Sigma_{xx}-\Sigma_{xt}\Sigma_{tt}^{-1}\Sigma_{tx}`),
 ],
 worked:B('For the existing teaching point A, μ₀ = (1, 2, 10), m₁ = (2, 0, 0), m₂ = (0, 4, 0), m₃ = (0, 0, 8). At t = 0.75 with τ = 0.5, Δt = 0.25 and μ = (1.5, 2.25, 10.125).','现有教学 A 点中，μ₀ = (1, 2, 10)、m₁ = (2, 0, 0)、m₂ = (0, 4, 0)、m₃ = (0, 0, 8)。t = 0.75、τ = 0.5 时，Δt = 0.25，μ = (1.5, 2.25, 10.125)。'),
 pitfall:B('Do not insert the spatial Gaussian’s one-half into the STG time kernel. It changes the effective temporal width.','不要把空间高斯的二分之一照搬进 STG 时间核，那会改变时间宽度的意义。'),
 task:B('Move time and watch position and opacity together. Then narrow the time window without changing the trajectory.','调整时间，同时观察位置与 opacity，再缩窄时间窗，确认轨迹本身没有被修改。'),
 code:[['motion',B('The shader evaluates the cubic trajectory.','着色器计算三次轨迹。')],['trace-decode',B('The CPU teaching evaluator exposes every intermediate temporal value.','CPU 教学求值器公开所有时间中间量。')]],refs:['stg','deform','native'],math:'stg',
 quizzes:[Q('A narrow temporal width mainly makes a primitive…','缩小时间宽度，主要会使点……',[B('Fade outside a shorter time interval','在更短时间区间之外淡出'),B('Move faster along the same trajectory','沿同一轨迹移动得更快'),B('Acquire more SH coefficients','增加 SH 系数')],0,[B('Correct. The window multiplies opacity; trajectory coefficients are unchanged.','正确，时间窗乘在 opacity 上，轨迹系数没有变。'),B('Trajectory speed comes from its polynomial derivatives and the time normalization.','轨迹速度来自多项式导数与时间归一化。'),B('Temporal width and appearance basis count are separate choices.','时间宽度与外观基函数数量是不同的选择。')])],
},
{
 id:'07-prepare-observations',title:B('Good training starts before the first gradient','好训练从第一步梯度之前就开始'),phase:2,lab:'data',
 goal:B('Assemble an image–camera–time training sample and prevent coordinate, resizing and evaluation mistakes.','组成图像—相机—时间训练样本，避免坐标、缩放和评估错误。'),
 hook:B('If a photograph says “look left” but its pose says “look right,” the optimizer receives contradictory instructions. More iterations do not repair a broken data contract.','如果照片实际朝左拍，相机外参却写着朝右，优化器收到的就是矛盾指令。多跑几轮不能修好错误的数据契约。'),
 steps:[
 S('A sample is more than an image','一个样本不只是一张图片','Associate each target image with camera intrinsics, an extrinsic convention, timestamp, image dimensions and preprocessing. Masks and exposure settings may also matter. Keep filenames and camera IDs traceable. Training compares pixels produced using this exact camera and time against this exact target.','每张目标图都要配内参、外参约定、时间戳、尺寸与预处理信息，必要时还要配遮罩和曝光设置。文件名与相机 ID 应能追溯。训练必须用这张图对应的相机和时间渲染，再与这张目标图逐像素比较。',String.raw`\mathcal D=\{(I_n,K_n,E_n,t_n)\}_{n=1}^{N}`),
 S('Estimate cameras and initialize geometry','估相机，再初始化几何','Structure-from-motion can estimate cameras and sparse 3D points from matched image features. Triangulation intersects observations from different viewpoints. Those points are useful Gaussian centers, but moving objects violate a static-scene assumption; dynamic pipelines require appropriate calibration, synchronization and initialization. Neither Gaussian training nor COLMAP is a universal pose-repair button.','运动恢复结构可通过图像特征匹配估计相机与稀疏三维点，三角化将多个视角的观测结合起来。稀疏点可作为 Gaussian 中心初值，但运动物体不满足静态场景假设；动态流程需要合适的标定、同步与初始化。Gaussian 训练和 COLMAP 都不是通用的相机修复按钮。'),
 S('Resize the camera with the image','缩图也必须缩内参','If pixel coordinates scale by r, focal lengths and principal-point coordinates scale by r too. Cropping first subtracts the crop origin from the principal point. The lab uses the continuous image-coordinate convention. Pixel-center formulas can include half-pixel offsets; match your resampler rather than mixing conventions.','像素坐标缩放 r 倍时，焦距与主点坐标也要缩放 r 倍。若先裁剪，则先从主点减裁剪起点。本实验使用连续图像坐标约定；具体重采样器的像素中心公式可能含半像素偏移，必须统一，不能混用。',String.raw`f'_x=r f_x,\quad f'_y=r f_y,\quad c'_x=r(c_x-x_0),\quad c'_y=r(c_y-y_0)`),
 S('Reserve unseen evidence','保留模型没见过的证据','Choose training and held-out cameras or times deliberately. Adjacent frames from the same video can be highly similar; a random frame split may test interpolation rather than new-view performance. Record what is held out. The browser’s allcam real fixture is not evidence of a held-out-camera benchmark.','有意识地划分训练与未见过的相机或时间。同一视频相邻帧很相似，随机按帧划分可能主要考察插值，而非新视角能力。要记录到底留出了什么。浏览器 allcam 真实模型不能作为留出相机评测的证据。'),
 S('Check a known point before training','先用已知点核对投影','Project a few known landmarks and overlay them on images. Check depth sign, image axes, focal units, quaternion order and time alignment. For a synthetic experiment, use a known teacher and separately held-out cameras/times so you can debug the loop before involving real reconstruction.','先把几个已知地标投影到图像上，叠加检查深度正负、图像轴方向、焦距单位、四元数顺序和时间同步。合成实验可用已知教师模型，并单独留出相机和时间，先调通训练闭环，再处理真实重建。'),
 ],
 worked:B('An 800 × 600 image with fx = 600 and cx = 400 becomes 400 × 300. With no crop, fx′ = 300 and cx′ = 200. A point at X/Z = 0.1 moves from u = 460 to u′ = 230. Keeping fx = 600 would incorrectly predict 260.','800 × 600 图像缩为 400 × 300，原 fx = 600、cx = 400，未裁剪时新 fx′ = 300、cx′ = 200。X/Z = 0.1 的点从 u = 460 变为 u′ = 230；若仍用 fx = 600，会错误预测为 260。'),
 pitfall:B('A file named cameras.json does not establish whether its rotation is world-to-camera or camera-to-world. Read the schema and validate a projection.','叫 cameras.json 的文件名不能说明旋转矩阵是世界到相机还是相机到世界。要读契约，并用投影验证。'),
 task:B('Resize the image. Toggle the intentionally wrong “keep original focal length” option and measure the pixel error.','缩放图像，切换故意错误的“保留原焦距”，观察像素误差。'),
 code:[['training-data',B('The browser experiment constructs fixed targets with explicit times and pixel coordinates.','浏览器实验用明确的时间与像素坐标构建固定目标。')]],refs:['colmap','gs','stg'],math:'projection',
 quizzes:[Q('Halve image dimensions with no crop. What should happen to focal lengths in pixels?','无裁剪地将图像尺寸减半，像素单位的焦距应怎样变？',[B('Also halve','也减半'),B('Stay fixed because the lens is unchanged','镜头没变，所以不变'),B('Double','加倍')],0,[B('Correct. The physical lens is unchanged, but the pixel coordinate scale changes.','正确，物理镜头没变，但像素坐标尺度变了。'),B('Physical focal length and focal length measured in resized pixels are different quantities.','物理焦距与缩放后以像素衡量的焦距是不同量。'),B('The new image has fewer pixels per corresponding displacement.','相同位置偏移在新图像中对应更少像素。')])],
},
{
 id:'08-follow-the-gradient',title:B('How a pixel tells a parameter to move','一个像素怎样告诉参数该往哪改'),phase:2,lab:'gradient',
 goal:B('Derive a complete scalar gradient and connect its chain rule to the real splat evaluator.','推完一条完整的标量梯度链，再对应真实 Splat 求值器。'),
 hook:B('A loss is like a height on a landscape. The derivative tells you which nearby direction goes uphill. To reduce loss, take a small step the other way.','把损失想象成地形高度，导数告诉你附近哪个方向上坡。要减小损失，就朝反方向迈一小步。'),
 steps:[
 S('Turn a color difference into a scalar','把颜色差变成一个数','For one scalar pixel, let prediction be Ĉ and target C*. Squaring their difference makes positive and negative errors both cost something. For RGB MSE, average the three squared errors; for a batch, average across pixels and samples as well. The averaging factors belong in the derivative too.','一个标量像素的预测为 Ĉ，目标为 C*。差值平方后，偏高偏低都有代价。RGB MSE 将三个平方误差取平均，批量训练还要对像素与样本取平均。求导时同样不能漏掉这些平均系数。',String.raw`L=(\widehat C-C^*)^2,\qquad\frac{\partial L}{\partial\widehat C}=2(\widehat C-C^*)`),
 S('Use a tiny renderer you can differentiate by hand','先用可以手算求导的微型渲染器','Fix the spatial Gaussian weight at one, use black background and one layer with color c. Its prediction is Ĉ = cα and α = sigmoid(β). Only β is learned here. The sigmoid derivative is α(1 − α): the same raw step has a smaller visible effect near saturation.','把空间权重固定为 1，使用黑背景、一个颜色为 c 的层。预测 Ĉ = cα，α = sigmoid(β)，这里只学 β。sigmoid 的导数是 α(1 − α)：接近饱和时，同样原始参数步长引起的可见变化更小。',String.raw`\widehat C=c\alpha,\quad\alpha=\frac1{1+e^{-\beta}},\quad\frac{\partial\widehat C}{\partial\alpha}=c,\quad\frac{\partial\alpha}{\partial\beta}=\alpha(1-\alpha)`),
 S('Multiply the local conversion rates','把局部换算率乘起来','A tiny change in β changes α, which changes Ĉ, which changes L. Multiply these three rates. This is the chain rule. With c = 0.8, β = 0 and target = 0.6, α = 0.5, Ĉ = 0.4 and the gradient is −0.08. Subtracting a negative gradient increases β and therefore opacity.','β 微变会引起 α 变化，再改变 Ĉ，最后改变 L。将三段变化率相乘就是链式法则。c = 0.8、β = 0、目标 0.6 时，α = 0.5、Ĉ = 0.4，梯度为 −0.08。减掉负梯度会增大 β，从而增大 opacity。',String.raw`\frac{\partial L}{\partial\beta}=2(\widehat C-C^*)\,c\,\alpha(1-\alpha),\qquad\beta_{new}=\beta-\eta\frac{\partial L}{\partial\beta}`),
 S('Extend from one path to a graph','从一条链扩展成计算图','A Gaussian position changes its projected center, its projected covariance, possibly its viewing direction and visibility. All differentiable paths must contribute. In the current direct-RGB evaluator the position gradient includes both projected-center and covariance paths. Overlapping splats also couple gradients through transmittance: a front opacity can reveal or hide a back color.','Gaussian 位置会改变投影中心、投影协方差，可能还改变观察方向与可见性。所有可微路径都要贡献梯度。当前直接 RGB 教学器的位置梯度同时包含中心和协方差两条路径。重叠点还通过透射率耦合：前点 opacity 可露出或遮住后点颜色。'),
 S('Check derivatives rather than trusting them','用数值检查导数','Perturb β by a small positive and negative ε and compare the loss difference. This finite difference should agree with the analytic gradient in a smooth region. Sorting, clipping, alpha thresholds and changing membership create branches; standard splat backward passes differentiate the active computation rather than a smooth global ordering function. Too small ε can also magnify roundoff.','给 β 加减一个小 ε，比较损失变化。平滑区域里，有限差分应与解析梯度接近。排序、裁剪、alpha 阈值和成员变化都会产生分支；常见 Splat 反向过程对当前活跃计算求导，而不是对全局排序函数做平滑求导。ε 太小还会放大舍入误差。',String.raw`g_{FD}=\frac{L(\beta+\varepsilon)-L(\beta-\varepsilon)}{2\varepsilon}`),
 ],
 worked:B('At the initial values, loss is 0.04. A step with η = 1 changes β from 0 to 0.08, α to about 0.51999, prediction to 0.41599 and loss to about 0.03386. No mysterious “AI instruction” is involved—just this chain of arithmetic.','在初值下，损失是 0.04。η = 1 的一步把 β 从 0 改为 0.08，α 约为 0.51999，预测约为 0.41599，损失约为 0.03386。这里没有神秘的“AI 指令”，就是这串算术。'),
 pitfall:B('One successful small step does not guarantee every learning rate or every later step will reduce the loss.','一次小步成功，不代表任意学习率或之后每一步都会让损失下降。'),
 task:B('Change β and learning rate. Compare the analytic gradient with the central finite difference, then inspect the candidate loss before applying a step.','修改 β 与学习率，对比解析梯度和中心有限差分，并在提交一步之前查看候选损失。'),
 code:[['trace-gradient',B('This longer derivative includes the real projection and compositing paths.','这段更完整的梯度包含实际投影与合成路径。')]],refs:['gs','train'],math:'training',
 quizzes:[Q('The gradient is negative. With positive learning rate, βnew = β − ηg will…','梯度为负，学习率为正，βnew = β − ηg 会使 β……',[B('Increase','增大'),B('Decrease','减小'),B('Always become zero','总是变成零')],0,[B('Correct. Subtracting a negative value increases the parameter.','正确，减去负数会使参数增大。'),B('The update uses the negative gradient, not the gradient itself.','更新使用负梯度方向，不是直接沿梯度。'),B('The update depends on the current value and step size.','更新结果取决于当前值与步长。')])],
}
];
