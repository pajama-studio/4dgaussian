import { B, S, Q } from './shared.mjs';
export const rendering = [
{
 id:'01-the-whole-story', title:B('From photographs to a scene you can move through','从照片，到可以自由观察的场景'), phase:0, lab:'pipeline',
 goal:B('Explain what is learned, what is rendered, and why the two use the same forward function.','说清楚模型学什么、渲染算什么，以及为什么二者共用同一个前向函数。'),
 hook:B('Imagine arranging translucent, colored pieces of cotton until every camera sees the right picture. Each piece has a small set of numbers. Training adjusts those numbers; rendering reads them.','想象你摆放很多带颜色、半透明的小棉团，让每台相机看到的画面都与照片一致。每团“棉花”由一组数字描述：训练修改数字，渲染读取数字。'),
 steps:[
 S('Start with the answer: pixels','先从答案出发：像素','A photograph is a grid of RGB triples. A camera pose says where it was taken; intrinsics say how rays map to pixels. For a dynamic scene, a timestamp says when it was taken. The photograph does not directly tell us the 3D position of every visible detail.','一张照片是一格格 RGB 数字。相机外参告诉我们在哪里拍、朝哪看，内参决定光线怎样映射到像素。动态场景还需要拍摄时间。照片本身并没有直接告诉我们每处细节的三维位置。'),
 S('Choose a representation','选择能被调整的表示','A Gaussian stores a center, a shape, opacity and appearance. Many overlapping Gaussians describe a scene without mesh connectivity. They are soft basis functions, not little solid balls with a hard surface. One Gaussian usually contributes to many pixels, and one pixel receives many Gaussians.','一颗 Gaussian 保存中心、形状、不透明度和外观。许多相互重叠的 Gaussian 不需要网格连接关系就能描述场景。它是柔和的基函数，不是有硬表面的小球。一颗点影响许多像素，一个像素也接收许多点。'),
 S('Run the forward trip','沿前向路线算一遍','For time t, evaluate the moving primitives. Transform them into camera space, project their centers and shapes, decide which pixels they cover, and combine their colors in depth order. R names this entire renderer. Θ is simply the collection of all adjustable numbers; K and E are intrinsics and extrinsics.','在时间 t 求出点的位置等状态，变换到相机空间，投影中心与形状，找出覆盖的像素，再按深度合成颜色。R 就是整套渲染计算的名字。Θ 只是所有可调数字的集合；K、E 分别表示相机内参与外参。',String.raw`\widehat I=R(\Theta;K,E,t)`),
 S('Reverse the question, not the video','反过来提问，不是倒放视频','Compare the rendered picture with the target. A loss turns their difference into a number. Derivatives tell us how a small parameter change would change that loss. An optimizer then proposes new parameters. Repeat this loop across training views and times. The backward pass does not uniquely reconstruct the world from one image.','比较渲染图和目标图，用损失把差别变成一个数。导数告诉我们微调某个参数时损失如何变化，优化器据此给出新参数。对多个视角和时间反复执行。一张图的反向传播并不能唯一还原整个世界。',String.raw`\Theta\longrightarrow\widehat I\longrightarrow L\longrightarrow\nabla_\Theta L\longrightarrow\Theta_{\rm new}`),
 S('Separate learning from playback','把训练与播放分清','A viewer needs trained parameters, a camera, a clock and a rasterizer. It usually does not need target photographs, gradients or optimizer state. Our real scene is a pretrained STG-Lite fixture. The small browser experiment is a separate synthetic training exercise; it is not how that real fixture was produced.','播放器需要训练参数、相机、时钟与光栅器，通常不需要目标照片、梯度或优化器状态。本项目真实场景来自预训练 STG-Lite 模型；浏览器小实验是另一套合成训练任务，不是那个真实模型的训练记录。'),
 ],
 worked:B('Follow Gaussian A at t = 0.5. Its center is (1, 2, 10). With f = 100 and principal point (64, 48), its screen center is (74, 68). We inspect pixel (74.5, 68.5), then combine it with Gaussian B. Every stage below is calculated from the existing teaching evaluator.','跟踪 t = 0.5 的 A 点：中心是 (1, 2, 10)，焦距 f = 100、主点 (64, 48)，屏幕中心就是 (74, 68)。观察像素 (74.5, 68.5)，再与 B 点合成。下方各阶段由现有教学求值器实时计算。'),
 pitfall:B('A lower training loss is evidence of fitting the observations. It does not by itself prove correct geometry or good unseen views.','训练损失下降说明更贴近训练观测，本身不能证明几何正确或新视角表现良好。'),
 task:B('Select each stage. Say which values belong to the model and which depend on this camera, time or pixel.','依次点击阶段，指出哪些值属于模型，哪些值依赖当前相机、时间或像素。'),
 code:[['trace-forward',B('The forward function consumes records and returns the pixel calculation.','forward 接收记录并返回像素计算过程。')]], refs:['gs','stg'], math:'pixel-lab',
 quizzes:[Q('What changes during ordinary playback?','普通播放时，什么在变化？',[B('Camera/time and evaluated state','相机、时间及求出的状态'),B('Parameters through backpropagation','通过反向传播修改训练参数'),B('The target training photographs','目标训练照片')],0,[B('Yes. The stored model can stay fixed while its evaluated state changes.','正确，存储模型可以不变，求值后的状态随输入变化。'),B('That would be training or editing. Playback does not require an optimizer.','那属于训练或编辑，播放不需要优化器。'),B('Target photographs are not required to render the trained model.','渲染训练好的模型不需要目标照片。')])],
},
{
 id:'02-a-soft-primitive', title:B('Why a Gaussian is a soft brush','为什么 Gaussian 像一支柔和的画笔'), phase:0, lab:'kernel',
 goal:B('Build the Gaussian weight from subtraction, division, squaring and an exponential; then understand covariance.','用减法、除法、平方和指数搭出高斯权重，再理解协方差。'),
 hook:B('A brush is strongest at its center and gradually fades outward. We need a rule that is smooth, symmetric, and easy to stretch or rotate.','画笔中心影响最强，向外逐渐变淡。我们需要一个平滑、左右对称，而且能拉伸与旋转的规则。'),
 steps:[
 S('Measure distance in brush widths','用“几倍宽度”测距离','Subtract the center μ from a location x. Divide by scale s: now the distance has no unit. At x = μ + s, the normalized distance is 1, regardless of whether the brush is one centimeter or one meter wide. Square it so the left and right sides receive the same value.','位置 x 减去中心 μ，再除以尺度 s，距离就变成没有单位的“几倍宽度”。无论画笔宽一厘米还是一米，x = μ + s 处都是 1。平方让左右两侧得到相同的值。',String.raw`d=\frac{x-\mu}{s},\qquad d^2=\left(\frac{x-\mu}{s}\right)^2`),
 S('Turn distance into a fading weight','把距离变成递减权重','The exponential e raised to a negative number gets smaller as that number becomes more negative. At the center, e⁰ = 1. The factor one half is the conventional Gaussian shape. At one, two and three scales, the weights are about 0.607, 0.135 and 0.011. These are pointwise weights, not percentages of probability inside a radius.','e 的指数越负，结果越小。中心处 e⁰ = 1；前面的二分之一是高斯常用的尺度约定。离中心 1、2、3 倍尺度时，权重约为 0.607、0.135、0.011。这些是该位置的权重，不是某半径内的概率百分比。',String.raw`G(x)=e^{-d^2/2}`),
 S('Stretch in three directions','沿三个方向分别拉伸','In the brush’s own axes, use three widths and add three squared distances. A wide axis penalizes displacement less, so the shape extends farther there. Multiplying by opacity changes strength; changing scale changes spatial reach. Our kernel has peak one and is not a probability density normalized to integrate to one.','在画笔自己的三个轴上，分别用三个宽度计算距离平方，再相加。宽的轴对偏移惩罚较小，因此延伸得更远。乘 opacity 改强度，改 scale 改空间范围。这里核的峰值是 1，并不要求像概率密度一样积分为 1。',String.raw`G=e^{-\frac12[(d_1/s_1)^2+(d_2/s_2)^2+(d_3/s_3)^2]}`),
 S('A matrix is a compact recipe','矩阵是把配方写紧凑','Rotate a world displacement into the brush’s axes with Rᵀ, divide by the widths, square and sum. The matrix Σ packages the rotated squared widths; its inverse undoes them. This explains the quadratic form instead of asking you to memorize it. Covariance here has units of length squared.','用 Rᵀ 把世界坐标中的偏移转回画笔自己的轴，除以宽度，再平方求和。Σ 把旋转后的宽度平方打包起来，Σ 的逆矩阵负责把它们除回去。这样就能理解二次型，而不只背公式。协方差的单位是长度平方。',String.raw`\Sigma=R\,\mathrm{diag}(s_1^2,s_2^2,s_3^2)R^T,\quad G=e^{-\frac12\mathbf d^T\Sigma^{-1}\mathbf d}`),
 S('Store unconstrained numbers safely','存储方便训练的数字','Learning a raw width could make it negative. Store ℓ instead and evaluate s = exp(ℓ). Store β and evaluate opacity with sigmoid. Store a quaternion for orientation and normalize it before constructing R. These activation steps are part of the model contract, not optional display effects.','直接学习宽度可能把它改成负数。改为存 ℓ，使用时算 s = exp(ℓ)。存 β，使用 sigmoid 得到 opacity。方向用四元数存储，构造 R 前先归一化。这些激活步骤属于模型契约，不是可选的显示特效。',String.raw`s=e^\ell,\qquad o=\frac1{1+e^{-\beta}}`),
 ],
 worked:B('At x − μ = 2, scale 2 gives G = exp(−0.5) ≈ 0.6065. Scale 1 gives exp(−2) ≈ 0.1353. With opacity 0.8, their uncapped alphas are 0.4852 and 0.1083.','偏移 x − μ = 2 时，尺度 2 得到 G = exp(−0.5) ≈ 0.6065；尺度 1 得到 exp(−2) ≈ 0.1353。若 opacity = 0.8，未截断 alpha 分别是 0.4852 和 0.1083。'),
 pitfall:B('The ellipsoid visualizes a level set of the kernel. A rasterizer draws its projected footprint; it is not shading an opaque ellipsoid surface.','椭球是核函数某个等值面的可视化。光栅器绘制它投影后的覆盖范围，并不是在给不透明椭球表面着色。'),
 task:B('Keep the sampled location fixed. Double scale, then halve opacity. Explain why those operations are not equivalent.','保持采样位置不变，先把尺度加倍，再把 opacity 减半，解释为什么这两个操作不等价。'),
 code:[['activation',B('Exponentials, opacity and normalized rotation are evaluated before projection.','指数、opacity 与归一化旋转都在投影前求值。')],['axes',B('Scaled rotation axes encode the covariance without a general matrix inverse.','缩放后的旋转轴用于表示协方差。')]],refs:['gs'],math:'gaussian',
 quizzes:[Q('At the center, what is G before multiplying by opacity?','中心位置乘 opacity 之前，G 等于多少？',[B('1 for every positive scale','任何正尺度下都等于 1'),B('The scale','等于尺度'),B('0','等于 0')],0,[B('Correct: the displacement is zero, so the exponent is zero.','正确：偏移为零，所以指数为零。'),B('Scale changes falloff, not the peak of this unnormalized kernel.','尺度改变衰减速度，不改变这个未归一化核的峰值。'),B('e⁰ is 1. Far from the center the weight approaches zero.','e⁰ 是 1，远离中心才逐渐趋于零。')])],
},
{
 id:'03-camera-to-ellipse',title:B('How a 3D brush lands on a 2D screen','三维画笔怎样落到二维屏幕上'),phase:0,lab:'camera',
 goal:B('Derive perspective from similar triangles and see why projecting shape needs a Jacobian.','从相似三角形推透视，理解为什么投影形状需要 Jacobian。'),
 hook:B('Hold a coin close to your eye, then move it away. Its size in the world stays fixed, but it covers fewer pixels. A Gaussian’s center and its footprint both need this perspective rule.','把硬币放在眼前再移远，世界中的尺寸没变，覆盖的像素却少了。Gaussian 的中心与覆盖范围都需要服从这个透视规律。'),
 steps:[
 S('Move the world into the camera’s frame','把世界搬到相机坐标系','Subtract the camera position, then rotate into its axes. This lesson uses +Z forward and +Y down, with a centered pinhole camera and no lens distortion. The wgpu host converts conventions for its right-handed view and clip transform. Column vectors, axis directions and matrix layout must be agreed before multiplying.','先减相机位置，再旋转到相机的坐标轴。本课采用 +Z 向前、+Y 向下、无畸变的居中针孔相机。wgpu 主机代码会转换为自己的右手观察与裁剪约定。列向量、坐标轴方向与矩阵布局必须先约定好。',String.raw`\mathbf p_c=R_{cw}(\mathbf p_w-\mathbf c)`),
 S('Divide by depth','为什么要除以深度','Similar triangles give horizontal image displacement divided by focal length equal to X divided by Z. Multiply by focal length, then add the principal point. Focal lengths here are measured in pixels. For X = 1, Z = 10, f = 100 and cx = 64, u = 74. A point at nonpositive depth is outside this teaching camera’s valid projection.','相似三角形给出“像面横向偏移 / 焦距 = X / Z”。乘焦距，再加主点位置，就得到像素坐标。这里焦距单位为像素。X = 1、Z = 10、f = 100、cx = 64 时，u = 74。深度非正的点不属于这套教学投影的有效范围。',String.raw`u=f_x\frac XZ+c_x,\qquad v=f_y\frac YZ+c_y`),
 S('Ask how a tiny move changes the image','微小位移会让屏幕变化多少','At fixed depth, increasing X by δX changes u by (f/Z)δX. Increasing depth makes the same X subtend a smaller angle: the local multiplier is −fX/Z². Collect these multipliers for u and v into J. A derivative is a local conversion rate, and the Jacobian is a table of those rates.','固定深度时，X 增加 δX，u 增加 (f/Z)δX。深度增大，同一个 X 张开的角度变小，其局部倍率是 −fX/Z²。把 u、v 对三个坐标的倍率收进 J。导数是局部换算率，Jacobian 就是这些换算率组成的表。',String.raw`J=\begin{bmatrix}f_x/Z&0&-f_xX/Z^2\\0&f_y/Z&-f_yY/Z^2\end{bmatrix}`),
 S('Project the axes, then rebuild the shape','投影各条轴，再重建形状','Write covariance as the sum of outer products of scaled axes aₖ. Each axis becomes Jaₖ in screen space. Adding their outer products gives C = JΣcJᵀ. This is a local linear approximation to perspective; a whole 3D Gaussian is not exactly a 2D Gaussian after perspective projection. Our shader adds 0.3 pixel² to each diagonal for a minimum footprint.','把协方差写成缩放轴 aₖ 的外积之和，每条轴投影后变成 Jaₖ，再把外积加起来就是 C = JΣcJᵀ。这是透视的局部线性近似；整个三维高斯经透视投影并不严格等于二维高斯。本项目 shader 给对角线各加 0.3 像素平方，提供最小覆盖。',String.raw`\Sigma_c=R_{cw}\Sigma R_{cw}^T,\qquad C=J\Sigma_cJ^T+0.3I_2`),
 S('Connect the traditional pipeline','接上传统图形管线','A triangle pipeline follows world → view → clip → perspective divide → NDC → viewport → rasterization. A splat pipeline shares the camera transforms but must also project covariance and evaluate a soft pixel weight. In this project, a small triangle quad encloses each ellipse; the fragment shader computes the actual falloff.','三角形管线经过世界 → 观察 → 裁剪 → 透视除法 → NDC → 视口 → 光栅化。Splat 共用相机变换，但还要投影协方差、计算柔和的像素权重。本项目用三角形组成的小四边形包住椭圆，片元着色器才计算真正的衰减。'),
 ],
 worked:B('At the optical axis, a spherical scale of 0.2 at Z = 10 and f = 100 gives a projected standard deviation of 2 pixels before filtering. Including 0.3 pixel² gives √4.3 ≈ 2.074 pixels. Doubling depth reduces the unfiltered width to 1 pixel, not the world-space scale.','在光轴上，球形尺度 0.2、Z = 10、f = 100 时，滤波前屏幕标准差为 2 像素。加 0.3 像素平方后得到 √4.3 ≈ 2.074 像素。深度加倍，滤波前宽度变成 1 像素，世界尺度没有变。'),
 pitfall:B('Never mix a world covariance with a camera-space Jacobian. Rotate covariance first; also check whether your screen Y points up or down.','不要把世界协方差直接与相机空间 Jacobian 混乘。先变换协方差，还要检查屏幕 Y 轴是向上还是向下。'),
 task:B('Double Z and predict both center displacement from cx and projected width. Then double focal length to compensate.','将 Z 加倍，预测中心距主点的偏移和投影宽度，再把焦距加倍补偿。'),
 code:[['projected-axis',B('The WGSL helper differentiates the clip-space quotient.','WGSL 辅助函数计算裁剪空间除法的变化率。')],['covariance',B('Projected axes produce the three independent entries of a 2 × 2 covariance.','投影轴构成二维协方差的三个独立元素。')]],refs:['gs','colmap'],math:'projection',
 quizzes:[Q('On the optical axis, double Z while keeping focal length fixed. The unfiltered width…','光轴上焦距不变、Z 加倍，滤波前的屏幕宽度……',[B('Halves','减半'),B('Doubles','加倍'),B('Stays fixed','不变')],0,[B('Correct: the local scale is f/Z. Variance is width squared, so it becomes one quarter.','正确，局部尺度是 f/Z。方差是宽度平方，因此变成四分之一。'),B('Farther objects cover fewer pixels; use f/Z rather than fZ.','越远覆盖像素越少，倍率是 f/Z 而不是 fZ。'),B('World size stays fixed; projected size changes.','世界尺寸不变，投影尺寸会变。')])],
},
{
 id:'04-many-splats-one-pixel',title:B('How overlapping splats become one pixel','许多重叠的 Splat 怎样变成一个像素'),phase:0,lab:'composite',
 goal:B('Compute front-to-back transmittance, explain ordering, and map the result to GPU blending.','算清前到后的透射率、解释排序，并对应 GPU 混合。'),
 hook:B('Look through two colored sheets of translucent film. The front sheet contributes color and hides part of the sheet behind it. A simple average cannot describe this.','透过两层半透明彩膜观察，前层贡献颜色，也遮住后层的一部分。简单取平均无法表达这种关系。'),
 steps:[
 S('Coverage is only a candidate test','覆盖测试只是在挑候选','Project a footprint and bound it with a rectangle. A pixel inside that rectangle is a candidate, not necessarily strongly covered. Evaluate its Gaussian weight using the inverse screen covariance and displacement from the projected center. Multiply by the evaluated opacity to get alpha.','先投影覆盖范围，用矩形包住。落在矩形里的像素只是候选，不一定贡献很大。用屏幕协方差的逆矩阵和像素到中心的偏移求高斯权重，再乘当前 opacity 得到 alpha。',String.raw`\alpha_i=o_i(t)\exp[-\tfrac12(\mathbf p-\boldsymbol\mu_i)^TC_i^{-1}(\mathbf p-\boldsymbol\mu_i)]`),
 S('Track how much is still visible','记录还有多少能透过去','Start with transmittance T = 1. A layer uses fraction α of the remaining visibility and passes fraction 1 − α onward. Before layer i, multiply all earlier 1 − α factors. This is why a rear layer has a smaller effective weight.','开始时透射率 T = 1。每层使用剩余可见份额中的 α，并把 1 − α 留给后面。因此第 i 层前，把前面所有 1 − α 相乘。后层的有效贡献由此减小。',String.raw`T_1=1,\quad T_i=\prod_{j<i}(1-\alpha_j),\quad w_i=T_i\alpha_i`),
 S('Add contributions and the background','加起各层与背景贡献','Multiply each layer’s RGB by its effective weight and add the remaining background. For red alpha 0.6 in front of blue alpha 0.5, the weights are 0.6 red, 0.2 blue and 0.2 background. Swap the layers and the weights become 0.3 red, 0.5 blue and 0.2 background. The same total coverage can produce a different color.','RGB 分别乘有效权重，再加剩余背景。红层 alpha 0.6 在蓝层 alpha 0.5 前面时，红、蓝、背景权重是 0.6、0.2、0.2。交换后成为 0.3、0.5、0.2。总覆盖相同，颜色却不同。',String.raw`\mathbf C=\sum_i T_i\alpha_i\mathbf c_i+\left[\prod_i(1-\alpha_i)\right]\mathbf b`),
 S('Two equivalent traversal conventions','两种顺序怎样对上','The derivation is front-to-back. Our graphics pass draws back-to-front with premultiplied over: new color = αc + (1 − α) old color. For the same layer order in space these produce the same result. Do not use front-to-back draw order with this ordinary over blend and expect the same image.','上面推导按前到后。我们的图形渲染通道按后到前绘制，使用预乘 over：新颜色 = αc + (1 − α)旧颜色。对于相同的空间层序，二者结果一致。不能把前到后的绘制顺序直接塞进这个普通 over 混合。',String.raw`\mathbf C_{\rm new}=\alpha\mathbf c+(1-\alpha)\mathbf C_{\rm old}`),
 S('Make it fast without changing the story','怎样加速这段计算','Tile binning collects splats relevant to a small screen region; sorting orders candidates; early termination can skip work after T becomes tiny. These are common compute-rasterizer techniques. Our baseline instead uses CPU visibility/global center-depth sorting plus GPU quads and blending. Center-depth order is an approximation for overlapping extended splats.','分块收集与小屏幕区域有关的点，排序决定先后；T 很小时可提前停止。这是计算型光栅器常见的加速方法。当前基线采用 CPU 可见性筛选、全局中心深度排序，加 GPU 四边形与混合。对于有体积且相互交叠的点，中心深度排序是一种近似。'),
 ],
 worked:B('With black background, red in front gives (0.6, 0, 0.2); blue in front gives (0.3, 0, 0.5). Contributions and residual background weights sum to 1. The lab lets you check both recurrence conventions numerically.','黑色背景下，红在前得到 (0.6, 0, 0.2)，蓝在前得到 (0.3, 0, 0.5)。各层贡献与剩余背景权重之和是 1。实验用数字核对两种递推约定。'),
 pitfall:B('Opaque depth writes can incorrectly suppress translucent layers behind a splat. Also distinguish straight RGB from RGB already multiplied by alpha.','不透明式深度写入可能错误压掉后面的半透明层。还要分清普通 RGB 与已经乘 alpha 的 RGB。'),
 task:B('Swap the layers. Observe which quantity stays fixed and which changes. Set front alpha to 1 and explain the back layer’s zero contribution.','交换前后层，观察什么不变、什么改变。把前层 alpha 设为 1，解释后层为什么贡献为零。'),
 code:[['fragment',B('The fragment shader emits RGB multiplied by alpha.','片元着色器输出乘了 alpha 的 RGB。')],['blend',B('The host chooses premultiplied-alpha blending.','主机选择预乘 alpha 混合。')],['sort',B('The host establishes the draw order.','主机负责建立绘制顺序。')]],refs:['gs','webgpu'],math:'compositing',
 quizzes:[Q('Why can swapping two translucent layers change the color?','为什么交换两个半透明层会改变颜色？',[B('Their transmittance weights change','它们的透射率权重发生变化'),B('Their stored RGB must change','存储 RGB 必须改变'),B('Their total alpha must always change','总 alpha 必然改变')],0,[B('Yes: the earlier layer attenuates the later layer.','正确，前层会衰减后层。'),B('Stored RGB can stay fixed; effective weights change.','存储 RGB 可以不变，变化的是有效权重。'),B('For two layers the product (1−αA)(1−αB) is unchanged by swapping.','两个层的剩余透射率乘积交换顺序后仍然一样。')])],
}
];
