import { B, S, Q } from './shared.mjs';
export const shipping = [
{
 id:'09-run-a-training-loop',title:B('Watch a model actually learn','看着一个模型真正学起来'),phase:2,lab:'train',
 goal:B('Run genuine updates, distinguish forward/backward/optimizer stages, and read what the loss does and does not prove.','运行真实更新，分清前向、反向、优化器，并正确解读损失曲线。'),
 hook:B('Now replace the single hand-written gradient with the existing two-Gaussian experiment. We keep targets fixed and watch three parameters adapt across three timestamps.','现在把手算的一条梯度换成已有的双 Gaussian 实验。目标保持不变，观察三个参数怎样适应三个时间的画面。'),
 steps:[
 S('Freeze the question before fitting the answer','先固定题目，再拟合答案','The teacher creates 12 × 10 target pixels at each of three times: 360 RGB samples. The student starts from a different record. Only Gaussian A’s initial x, linear x-motion and opacity logit change. Gaussian B, camera, shape, other motion terms and target images remain fixed. This bounded task makes the cause of each improvement inspectable.','教师在三个时间各生成 12 × 10 个目标像素，总共 360 个 RGB 样本。学生从不同记录开始。只改变 A 点初始 x、x 方向线性运动和 opacity logit；B 点、相机、形状、其他运动项和目标图保持固定。这样才能看清每次改善的原因。'),
 S('Forward, loss, backward, update','前向、损失、反向、更新','Evaluate all samples using the current records. Average their MSE. Sum the analytic parameter gradients with the same averaging. Compute all new values from the same old records, then commit them together. Changing the first parameter before calculating the second gradient would be a different algorithm.','用当前记录求所有样本，平均得到 MSE，用相同平均方式汇总解析梯度。所有新参数都根据同一份旧记录计算，再一起提交。如果改完第一个参数才算第二个梯度，就成了另一种算法。',String.raw`L=\frac1{3N}\sum_{n=1}^{N}\sum_{h\in\{R,G,B\}}(\widehat C_{nh}-C^*_{nh})^2,\qquad\theta_j'=\theta_j-\eta\,\partial_{\theta_j}L`),
 S('One iteration is not one explanatory click','一次迭代不是一次讲解点击','The eight-stage experiment separates reading a sample, evaluating time, projection, rendering, loss, gradient, candidate update and updated render. A parameter update is committed only at the last stage. Going backward restores the exact old record. The simpler controls on this course page commit one whole iteration at a time.','八阶段实验把读取样本、时间求值、投影、渲染、损失、梯度、候选更新、更新后渲染分开。只有最后一步提交参数，回退会恢复旧记录。本课程页的简化按钮每次提交一整轮迭代。'),
 S('SGD and Adam use the same gradients differently','SGD 与 Adam 怎样使用同一批梯度','The browser lab uses full-batch SGD. The Python toy trainer uses Adam, which keeps moving averages of gradients and their squares, corrects their initial bias and rescales updates. Here k is the update number, g is the gradient, b₁ and b₂ are averaging decay rates, and ε prevents division by zero. A checkpoint for resuming Adam needs those states and the step counter, not just exported Gaussian attributes. Learning rates often differ by parameter group.','浏览器实验使用全批量 SGD。Python 合成训练器使用 Adam，维护梯度及梯度平方的移动平均，修正初始偏差，再对更新幅度重新缩放。这里 k 是更新步数，g 是梯度，b₁、b₂ 是平均的衰减率，ε 防止除零。恢复 Adam 训练的检查点需要这些状态和步数，而不只是导出的 Gaussian 属性。不同参数组也常使用不同学习率。',String.raw`\begin{gathered}m_k=b_1m_{k-1}+(1-b_1)g_k,\quad v_k=b_2v_{k-1}+(1-b_2)g_k^2\\\widehat m_k=\frac{m_k}{1-b_1^k},\quad\widehat v_k=\frac{v_k}{1-b_2^k},\quad\theta_k=\theta_{k-1}-\eta\frac{\widehat m_k}{\sqrt{\widehat v_k}+\epsilon}\end{gathered}`),
 S('Read loss alongside the image and the contract','把损失、图像和任务边界一起看','Lower MSE should correspond to changes you can see in the error image. Real pipelines may combine L1, SSIM-based and regularization terms; that is not the same objective as this MSE lab. Our fixed two-Gaussian task has no density control, SH training or real-scene reconstruction. Convergence here demonstrates the loop, not production reconstruction quality.','MSE 下降应能在误差图中找到对应变化。真实流程可能组合 L1、基于 SSIM 的损失及正则项，与本实验 MSE 不是同一个目标。双 Gaussian 固定任务不包含密度控制、SH 训练或真实重建。这里收敛证明训练闭环成立，不代表生产重建质量。'),
 ],
 worked:B('At learning rate 5, the deterministic initial MSE is about 0.004031. After 50 full-batch updates it is about 0.001795. These are recomputed from the renderer; the curve is not a prerecorded animation. Different rates can behave differently.','学习率 5 时，确定性初始 MSE 约为 0.004031；50 次全批量更新后约为 0.001795。这些数值由渲染器重新计算，曲线不是预先录好的动画。其他学习率可能有不同表现。'),
 pitfall:B('Do not optimize against targets regenerated from the student after each step. That moves the goal and destroys the meaning of the fitting experiment.','不要每步用学生的新参数重新生成目标，那会不断移动目标，破坏拟合实验的意义。'),
 task:B('Apply one update, rewind it, and confirm the same loss returns. Then run 50 updates and inspect all three timestamps.','执行一步并回退，确认恢复同一损失；再训练 50 步，检查三个时间点。'),
 code:[['training-batch',B('The batch computes actual image loss and accumulated gradients.','批量求值计算真实图像损失与累积梯度。')],['training-step',B('The update is simultaneous and uses pre-update gradients.','更新同步提交，梯度来自更新前状态。')],['train-loop',B('The separate Python experiment uses PyTorch autograd and Adam.','独立 Python 实验使用 PyTorch 自动求导与 Adam。')]],refs:['adam','train'],math:'training-lab',
 quizzes:[Q('Which data must stay fixed while fitting this synthetic task?','拟合这个合成任务时，什么必须保持固定？',[B('Teacher target images','教师生成的目标图'),B('The three trainable parameters','三个可训练参数'),B('The predicted images','预测图')],0,[B('Correct. A fixed target gives the loss a stable meaning.','正确，固定目标使损失有稳定含义。'),B('Those are precisely the numbers training adjusts.','训练调整的恰好就是这些数。'),B('Predictions should change when updated parameters change the render.','参数更新改变渲染后，预测也应变化。')])],
},
{
 id:'10-grow-and-prune',title:B('Why training sometimes adds or removes Gaussians','为什么训练有时要增删 Gaussian'),phase:2,lab:'density',
 goal:B('Distinguish optimizing existing parameters from changing representation capacity, without treating clone/split as exact preservation.','分清调整现有参数与改变表示容量，并理解复制或分裂并不严格保图像不变。'),
 hook:B('One huge soft brush can cover a region but cannot describe every small edge. Moving that one brush forever may not solve the problem; sometimes you need more brushes in the right places.','一支巨大的柔和画笔能覆盖大区域，却难以描出每个细小边缘。永远调整同一支画笔不一定能解决问题，有时需要在合适位置增加画笔。'),
 steps:[
 S('Parameters and capacity are separate','参数与容量是两件事','Gradient descent changes values for existing primitives. Density control changes how many primitives exist and where optimization effort is available. Regions with persistent errors or large accumulated screen-space position gradients can motivate extra primitives, but large error alone does not identify its cause: pose mistakes or exposure differences can also create it.','梯度下降改变已有点的数值，密度控制改变点的数量与可分配的表达能力。持续误差或较大的累积屏幕位置梯度可能提示增加点，但大误差本身不能说明原因：相机错误、曝光差异也可能导致误差。'),
 S('Clone small detail; split broad coverage','复制细节点，拆分大覆盖','In the original 3DGS strategy, gradient statistics and scale help distinguish clone and split candidates. A clone adds capacity near a small primitive; a split samples smaller children around a larger one and removes the parent. Exact thresholds, scheduling and child initialization belong to that implementation. STG has its own temporal and guided-sampling strategies.','原始 3DGS 策略结合梯度统计与尺度区分复制和分裂候选。复制在小点附近增加容量；分裂在较大点附近采样更小子点并去掉父点。具体阈值、时机与初始值由实现决定。STG 还有自己的时间与引导采样策略。'),
 S('Duplicating a layer changes opacity','复制一层会改变不透明度','Two identical coincident layers with alpha a leave transmittance (1 − a)². Their combined alpha is 1 − (1 − a)², which is larger than a except at endpoints. For a = 0.5, the result is 0.75. Therefore cloning cannot be described as simply copying a point with no image change.','两个完全相同且重合的 alpha = a 层，剩余透射率是 (1 − a)²，合成 alpha 为 1 − (1 − a)²，除端点外都比 a 大。a = 0.5 时结果是 0.75。所以不能把复制解释成“复制一颗点而图像完全不变”。',String.raw`\alpha_{\rm pair}=1-(1-a)^2`),
 S('A one-pixel correction is not a global solution','单像素修正不是全局解','To preserve an original alpha A at one pixel using two equal children, solve (1 − a)² = 1 − A, giving a = 1 − √(1 − A). At A = 0.5 this is about 0.2929, not 0.25. Across an entire Gaussian footprint, a constant opacity adjustment generally cannot preserve every pixel, especially when children move or shrink. Optimization must refine the result.','若用两个相同子层在某个像素保持原 alpha A，解 (1 − a)² = 1 − A，得 a = 1 − √(1 − A)。A = 0.5 时约为 0.2929，而不是 0.25。整个高斯覆盖范围里，单个固定 opacity 调整通常不能保证每个像素都不变，更何况子点还移动、缩小了。后续需要继续优化。',String.raw`a=1-\sqrt{1-A}`),
 S('Remove unhelpful work and maintain optimizer state','去掉无用开销，维护优化器状态','Pruning can remove persistently low-opacity or otherwise unsuitable primitives according to the selected method. Adding/removing rows must keep every parameter array, optimizer moment array and identity mapping aligned. Opacity resets and growth schedules affect optimization; they are not ordinary inference-time operations. Our course density lab is a compositing thought experiment, not a density-control implementation.','剪枝按具体方法去除长期低 opacity 或不合适的点。增删行时，所有参数数组、优化器矩数组与身份映射都必须对齐。opacity 重置与增长调度会改变优化过程，不是普通推理操作。本课密度实验是合成原理实验，不是密度控制实现。'),
 ],
 worked:B('At one pixel with A = 0.5: duplicate unchanged → 0.75; assign each child A/2 → 0.4375; use 1 − √(1 − A) for each child → 0.5. The third equality is only the stated coincident one-pixel setup.','某像素 A = 0.5：不变地复制得到 0.75；每个子层分 A/2 得 0.4375；每个设为 1 − √(1 − A) 才得到 0.5。第三个等式只针对这里约定的重合单像素情形。'),
 pitfall:B('More Gaussians are not automatically better: count, overlap, memory and sorting cost all matter.','点越多不一定越好，数量、重叠、内存与排序成本都要考虑。'),
 task:B('Compare the three duplication rules. Explain why dividing opacity by two does not exactly preserve alpha.','对比三种复制规则，解释为什么 opacity 除以二不能严格保持 alpha。'),
 code:[['fragment',B('The same alpha law explains why adding primitives changes their combined footprint.','同一个 alpha 合成规律说明增加点为何改变合成覆盖。')]],refs:['density','stg'],math:'training',
 quizzes:[Q('Two coincident layers each have alpha 0.5. Their combined alpha is…','两个重合层各有 alpha 0.5，合成 alpha 是……',[B('0.75','0.75'),B('1.0','1.0'),B('0.5','0.5')],0,[B('Correct: one minus the remaining quarter of transmittance.','正确，剩余透射率为四分之一，合成 alpha 就是四分之三。'),B('Alpha is not added directly; the rear layer is attenuated.','alpha 不是直接相加，后层会受到衰减。'),B('Duplicating an unchanged layer increases coverage.','不变地复制一层会增加覆盖。')])],
},
{
 id:'11-export-the-contract',title:B('A model file is a promise to the renderer','模型文件是对渲染器的一份约定'),phase:3,lab:'record',
 goal:B('Trace a trained number through a binary record, activation and shader field; distinguish export from a resumable checkpoint.','把训练数字追踪到二进制记录、激活值和 shader 字段，分清导出文件与训练检查点。'),
 hook:B('A PLY can load successfully and still render nonsense. The reader may understand the bytes while misunderstanding what the numbers mean.','PLY 成功读取并不代表画面正确。读取器可能看懂了字节，却误解了数字的含义。'),
 steps:[
 S('Container is not semantics','容器不是语义','PLY declares properties, types and element counts. It does not impose a universal Gaussian schema. A field called f_dc_0 might mean an SH DC coefficient in one exporter and direct red in another. Names alone are insufficient: record activations, color conventions, orientation, units and time normalization.','PLY 声明属性、类型与元素数量，但没有规定通用 Gaussian 语义。某个导出器的 f_dc_0 可能是 SH 的 DC 系数，另一个却直接存红色值。不能只看名字，还要记录激活、颜色约定、旋转、单位与时间归一化。'),
 S('Map this exact 32-float row','逐项对上本项目 32 个浮点数','Our STG-Lite teaching row stores center at 0–2, time center/width log at 3–4, placeholder normals at 5–7, motion at 8–16, direct RGB at 17–19, opacity logit at 20, scale logs at 21–23, wxyz quaternion at 24–27 and its four linear coefficients at 28–31. This is 128 bytes per decoded row; compressed file size is a different quantity.','本教学 STG-Lite 行中：0–2 中心，3–4 时间中心与宽度对数，5–7 占位法线，8–16 运动，17–19 直接 RGB，20 opacity logit，21–23 尺度对数，24–27 为 wxyz 四元数，28–31 为其四个线性系数。一行解码后 128 字节，压缩文件大小是另一回事。',String.raw`32\ \mathrm{floats}\times4\ \mathrm{bytes}=128\ \mathrm{bytes/record}`),
 S('Activate exactly once','激活只能按约定做一次','The Python trainer learns RGB logits internally and exports sigmoid RGB; the viewer must therefore read those exported RGB values directly. Opacity is exported as a logit and scales as logs, so the viewer still activates those. Applying sigmoid or an SH DC transform to already exported direct RGB changes the intended image.','Python 训练器内部学 RGB logits，导出前已算 sigmoid，因此播放器直接读导出的 RGB。opacity 导出的是 logit，scale 导出的是对数，播放器仍需激活它们。对已导出的直接 RGB 再做 sigmoid 或 SH DC 变换，会改变原本图像。'),
 S('Validate before uploading to the GPU','上传 GPU 之前先验证','Check header order, float type, little-endian payload, record count, payload length and finite values. CPU and GPU buffers can have different layouts; map them explicitly instead of assuming a PLY row is a shader struct. A round-trip export/decode test and a known-pixel reference are stronger than merely seeing a picture.','检查头部属性顺序、float 类型、小端载荷、记录数量、载荷长度与有限值。CPU 与 GPU 缓冲布局可能不同，应明确映射，不能假定 PLY 行就是 shader 结构体。导出再解码的往返检查，以及已知像素对照，比“看见画面”更可靠。'),
 S('Keep deployment artifacts and training state distinct','分清部署产物与训练状态','An inference export stores what the renderer needs. A resumable training checkpoint additionally needs optimizer states, iteration, schedules and any other learned components such as deformation networks or feature decoders. For this project’s native exercise, export model.ply and the matching cameras.json, then run the existing StgPass host.','推理导出保存渲染需要的内容；恢复训练还需要优化器状态、迭代数、调度及其他学习组件，例如形变网络或特征解码器。本项目原生练习导出 model.ply 和配套 cameras.json，再运行已有的 StgPass 主机。'),
 ],
 worked:B('Select field 20 below. The stored β is ln(4) ≈ 1.386294 and base opacity is sigmoid(β) = 0.8. Select scale_0: stored ln(0.2) ≈ −1.609438 means world-space scale 0.2, not a negative width.','下方选择字段 20：存储 β = ln(4) ≈ 1.386294，基准 opacity = sigmoid(β) = 0.8。选择 scale_0：存储 ln(0.2) ≈ −1.609438，意味着世界尺度 0.2，而不是负宽度。'),
 pitfall:B('Four-dimensional time-varying data does not guarantee compatibility between two files labeled “4DGS.” Choose the decoder by representation contract.','都叫“4DGS”不意味着文件互相兼容，解码器必须按表示契约选择。'),
 task:B('Choose center, motion, direct RGB, opacity and scale fields. For each, explain whether it is copied, activated or evaluated using time.','依次选中心、运动、直接 RGB、opacity 与尺度，说明它是直接复制、激活，还是结合时间求值。'),
 code:[['export',B('The exporter makes the raw-versus-activated distinction explicit.','导出器明确区分原始值与激活值。')],['trace-ply',B('The teaching encoder writes the same little-endian property sequence.','教学编码器写入相同的小端属性序列。')]],refs:['stg'],math:'pixel-lab',
 quizzes:[Q('The file stores scale_0 = ln(0.2). Which runtime scale is correct?','文件里 scale_0 = ln(0.2)，正确运行时尺度是多少？',[B('exp(scale_0) = 0.2','exp(scale_0) = 0.2'),B('scale_0 ≈ −1.609','scale_0 ≈ −1.609'),B('sigmoid(scale_0)','sigmoid(scale_0)')],0,[B('Correct. The contract specifies log-scale storage.','正确，契约规定存储 log-scale。'),B('The negative value is a logarithm, not the physical width.','这个负数是对数，不是物理宽度。'),B('Sigmoid is the opacity activation here; scale uses exp.','这里 opacity 用 sigmoid，尺度用 exp。')])],
},
{
 id:'12-build-and-explain',title:B('Train, export, render—and defend every step','训练、导出、渲染，并解释每一步'),phase:3,lab:'budget',
 goal:B('Run the repository’s end-to-end exercise and explain how to integrate and profile its Rust/wgpu render pass.','跑通仓库端到端练习，说清 Rust/wgpu 渲染通道的接入与性能测量。'),
 hook:B('You are ready when you can explain a bad image, not only display a good one. This final lesson turns the chain into a reproducible exercise and a debugging conversation.','能显示好看的画面还不够，能解释坏画面的原因才算真正掌握。最后一课把整条链变成可复现练习与调试思路。'),
 steps:[
 S('Run the smallest complete experiment','先跑最小完整实验','Use the repository commands below. The Python exercise creates a synthetic teacher, fits five Gaussians with Adam, evaluates separate synthetic views/times and exports a PLY plus cameras. It needs Python with PyTorch and NumPy. The native Rust example needs a working wgpu adapter. This exercise is distinct from the two-Gaussian browser SGD lab.','运行下方仓库命令。Python 练习创建合成教师，用 Adam 拟合五颗 Gaussian，对单独的合成视角与时间评估，并导出 PLY 和相机。需要带 PyTorch、NumPy 的 Python 环境；Rust 原生示例需要可用 wgpu 适配器。它与浏览器双 Gaussian SGD 实验不同。'),
 S('Integrate a pass, not a second engine','把渲染通道接入引擎','Let the host own device, queue, camera, clock and render targets. The Gaussian pass owns model buffers and its rendering resources. Each frame evaluates visibility/order, updates per-frame camera/time and index data, then encodes a pass into the host’s command flow. The current StgPass is an integration starting point; it is not evidence of a completed integration into another engine repository.','设备、队列、相机、时钟与渲染目标由主引擎管理，Gaussian 通道管理模型缓冲与自己的渲染资源。每帧求可见性和顺序，更新相机、时间与索引数据，再把渲染通道编码到主机命令流。当前 StgPass 是接入起点，不等于已经集成到另一个引擎仓库。'),
 S('Keep common math separate from backend mechanics','分离通用数学与后端机制','Time decoding, coordinate contracts, SH conventions and expected pixel behavior can be shared across backends. Buffer alignment, shader language, synchronization, sorting primitives and render/compute dispatch belong to backend implementation. CUDA compute splatting and wgpu quads can implement related image formation with different scheduling. Matching outputs requires matching approximations as well as formulas.','时间解码、坐标契约、SH 约定与像素预期可以跨后端共享。缓冲对齐、着色语言、同步、排序原语及渲染或计算调度由后端实现。CUDA 计算 Splat 与 wgpu 四边形可以实现相关成像，但调度方式不同。对齐输出不仅要对公式，还要对近似。'),
 S('Measure the actual bottleneck','测出真正瓶颈','Record decode time, CPU visibility/sort time, upload bytes, GPU pass time, resolution and visible splat count. High overlap can dominate fragment work even with a modest point count. Timing an asynchronous API call is not GPU execution time. Use supported timestamp queries or carefully scoped measurement; avoid a blocking readback every frame.','记录解码时间、CPU 可见性与排序时间、上传字节数、GPU 通道时间、分辨率与可见点数。即使点数不多，重叠严重也可能使片元工作量很大。异步 API 调用耗时不等于 GPU 执行耗时。使用受支持的时间戳或明确范围的测量，避免每帧阻塞回读。'),
 S('Debug in the same order that pixels are made','沿成像顺序调试','Wrong motion: inspect normalized time and decoded coefficients. Mirrored scene: inspect camera axes and matrix conventions. Giant splats: inspect log-scale activation and covariance units. Dark colors: inspect direct RGB versus SH and premultiplied blending. Popping: inspect visibility and order changes. Low FPS: measure stages before replacing the rasterizer.','运动错误查归一化时间与解码系数；镜像场景查相机轴和矩阵约定；巨大点查 log-scale 激活与协方差单位；颜色发黑查直接 RGB / SH 与预乘混合；跳变查可见性与排序变化；低帧率先分阶段测量，再决定是否替换光栅器。'),
 ],
 worked:B('A decoded 32-float row is 128 bytes. At 108,317 splats this is about 13.22 MiB. Uploading only 108,317 32-bit indices costs about 0.413 MiB per frame, rather than resending the complete records. These numbers exclude duplicate CPU copies, GPU padding, render targets and other resources.','每行 32 个 float 是 128 字节，108,317 颗约占 13.22 MiB。每帧只上传同数量的 32 位索引约需 0.413 MiB，无需重传整套记录。这些数字不包含 CPU 重复副本、GPU 填充、渲染目标与其他资源。'),
 pitfall:B('A small training demo, a pixel equivalence check and a research viewer are different pieces of evidence. State which one you have actually run when discussing readiness.','微型训练、像素一致性验证与研究播放器是不同证据。讨论掌握程度时，要说清自己实际跑过哪一个。'),
 task:B('Estimate record memory and index bandwidth. Then complete the capstone checklist and explain one possible failure at each boundary.','估算记录内存与索引带宽，再完成毕业练习清单，解释各个边界可能发生的一种错误。'),
 code:[['host-camera',B('The existing pass adapts the host camera and updates ordering.','现有通道适配主机相机并更新排序。')],['quad',B('The GPU expands each projected splat into a quad.','GPU 将投影点扩展为四边形。')]],refs:['webgpu','gs'],math:'checklist',
 commands:'python scripts/train_toy_stg.py --device cpu --steps 400 --output artifacts/toy-training\ncargo run --example native_smoke -- artifacts/toy-training/model.ply artifacts/toy-training/cameras.json artifacts/toy-native\ncargo run --example mini_compute\nnpm run check',
 deliverables:[B('Read training-report.json: learned/fixed fields, initial/final MSE, held-out synthetic MSE and seed.','读 training-report.json：学了什么、固定了什么、初末 MSE、留出合成 MSE 与随机种子。'),B('Inspect native output at multiple timestamps; verify time and camera conventions before judging image fidelity.','检查多个时间的原生输出，先核对时间与相机约定，再判断图像保真度。'),B('Run mini_compute and inspect its CPU/GPU pixel comparison; explain that it has no GPU tile bins or sorter.','运行 mini_compute 并检查 CPU/GPU 像素对照，说明它没有 GPU 分块或排序器。'),B('Explain the full pipeline without opening the formulas, then use the code links to verify your explanation.','不看公式讲完整条管线，再用源码链接核对自己的解释。')],
 quizzes:[Q('GPU rendering is asynchronous. What is a useful timing measurement?','GPU 渲染是异步的，哪种计时更有用？',[B('A supported GPU timestamp around the pass','在通道前后使用受支持的 GPU 时间戳'),B('Only time the queue submission call','只计队列提交调用'),B('Infer speed from compressed PLY size','从压缩 PLY 大小推测速度')],0,[B('Correct. Also report resolution, splat count and the exact measured scope.','正确，还要报告分辨率、点数与具体测量范围。'),B('Submission time measures CPU work and submission overhead, not completed GPU execution.','提交时间衡量 CPU 工作与提交开销，不是完整 GPU 执行。'),B('Compressed file size describes delivery, not runtime GPU work.','压缩文件大小描述传输，不等于 GPU 工作量。')])],
}
];
