export const englishFamilies = {
deformation: [
 'Interface definition for another model family','How can time affect a Gaussian without stored motion polynomials?',
 [
  ['Store a reference state','A canonical Gaussian G⁰ may contain a center, log-scales, quaternion, opacity, and other parameters. Canonical space is a shared reference, not necessarily the first captured frame.'],
  ['Let a function read position and time','Fθ is a function with learnable parameters θ. A basic network layer multiplies an input by a matrix, adds a bias, and applies a nonlinearity such as componentwise ReLU=max(0,x). Wu et al. also use spatiotemporal feature encoding; this small example does not replace their model.'],
  ['Decode each output head in its own parameter domain','One possible example adds displacement to position, adds a log-scale increment before exp, and adds a quaternion increment before normalization. These are illustrative choices, not universal rules for deformation repositories.'],
  ['Return the time-dependent result to the shared 3D rendering interface','Whether time is handled by a network or a polynomial, current μ,Σ,o and appearance can feed projection, sorting, and composition. A network additionally requires stored weights, inference, and a decoder matching training.'],
 ],
 'For just the log-scale head, let s0=2 and Δℓ=ln1.5.',
 [['This standalone teaching function demonstrates decoding in three parameter domains. It is not connected to StgPass and is not a Wu 4D-GS model loader.','Δμ,Δℓ,Δq ↔ deltaPosition,deltaLogScale,deltaQuaternion']],
 'Renaming a network checkpoint to STG PLY does not make it loadable. Network weights, feature grids, and their execution are still required.',
 'Does adding 0.1 to log-scale add 0.1 to physical scale?','No. Physical scale is multiplied by exp(0.1)≈1.10517.',
],
'four-dimensional': [
 'Definition of a joint variable','What does the fourth dimension of a native 4D Gaussian actually contain?',
 [
  ['Put space and time in one column','The first three entries are spatial coordinates and the fourth is time. The mean also has four components. The units may differ, so covariance blocks have different units too.'],
  ['Write every average product of deviations','Each entry of the 4×4 matrix averages the product of two coordinate deviations. The three upper-right entries describe how each spatial coordinate varies with time.'],
  ['Group the entries into four blocks','A is the 3×3 spatial covariance, b is the 3×1 space-time cross-covariance, and c is temporal variance. bᵀ is 1×3, so the blocks form a 4×4 matrix.'],
  ['Require a nondegenerate four-dimensional shape','The joint covariance must be positive definite. The following derivation shows this is equivalent to c>0 and positive-definite S=A−bbᵀ/c. The magnitude of b cannot be chosen arbitrarily.'],
 ],
 'An example with correlation only between x and time: A=diag(2,1,1), b=(1,0,0), c=1.',
 [['The teaching function takes A,b,c explicitly. The current 32-float STG record has no such blocks; r0..r7 cannot simply be reinterpreted as them.','A,b,c ↔ teaching arguments with the same names; absent from the current model format']],
 'Appending a timestamp to a 3D Gaussian does not create a joint 4D Gaussian. Nor can an ordinary 3D quaternion directly represent a general 4D rotation.',
 'What does b=0 mean?','Space and time are independent in this joint Gaussian model; fixing time does not move the spatial center.',
],
conditional: [
 'Remove correlation → condition the distribution','Why subtract bbᵀ/c rather than use A when time is fixed?',
 [
  ['Subtract the means from both variables','Let X be the 3D spatial offset and T the temporal offset. Both have mean zero, with covariance blocks A,b,c.'],
  ['Subtract a time-dependent component from X','Define U=X−kT and seek a residual uncorrelated with T. Expand the expected product and set it to zero to solve for k, without memorizing the conditional-mean formula.'],
  ['Expand the residual covariance','Take expectations of all four terms. Both cross terms subtract bbᵀ/c and the final term adds it once, leaving one net subtraction.'],
  ['Use the special structure of a joint Gaussian','For general distributions, uncorrelated does not imply independent. A Gaussian exponent is quadratic; the next block factorization separates its U and T terms, proving that fixing T leaves U’s distribution unchanged.'],
  ['Add the fixed-time displacement back to the center','X=U+(b/c)T. Once time is fixed, the second term is a translation. The residual remains a zero-mean Gaussian with covariance S.'],
 ],
 'The same reasoning works for scalar space: Var(X)=2, Cov(X,T)=1, Var(T)=1. Conditioning on T=3 moves the mean to 3 and reduces variance from 2 to 1.',
 [['The function computes shifted mean and A[i][j]−b[i]*b[j]/c entry by entry. It verifies the derivation without modifying the STG renderer.','Conditional mean ↔ mean; S ↔ covariance']],
 'Conditioning and marginalizing are different. Ignoring time gives spatial marginal covariance A; knowing time gives the smaller conditional covariance S.',
 'Does the conditional covariance of one fixed 4D Gaussian vary with t?','No. It depends only on A,b,c. Its center moves linearly with t and its temporal weight changes.',
],
'four-factorization': [
 'Complete proof by block-matrix completion of squares','Why does the kernel factor into a spatial conditional kernel and a temporal kernel?',
 [
  ['Construct an invertible coordinate change','Set k=b/c and S=A−bbᵀ/c. The block triangular matrix L combines residual U and time T into X=U+kT.'],
  ['Multiply the blocks to verify the covariance','Multiply the first two matrices, then Lᵀ. The upper-right block is kc=b and upper-left is S+kckᵀ=A. This also proves positive definiteness of the joint covariance when c>0 and S is positive definite.'],
  ['Substitute the inverse into the quadratic form','Inverting a product reverses its order. Since L⁻¹[X;T]=[X−kT;T], the cross terms disappear without explicitly writing a 4×4 inverse.'],
  ['Turn addition in the exponent into multiplication of kernels','Multiply by −1/2 and exponentiate, using exp(a+b)=exp(a)exp(b). The first factor is centered at μx+kT; the second depends only on time.'],
  ['Probability-density constants factor too, if needed','det(L)=1, so det(Σ4)=det(S)c. The density normalization separates into 3D and 1D factors. The rendering kernels on this page do not include these constants.'],
 ],
 'In the previous example, let t=1 and the spatial point be (1,0,0). It lies at the conditional center, so its spatial kernel equals 1.',
 [['Keep timeWeight=exp(−0.5*dt²/c). Pass it along with conditional center and covariance to the subsequent 3D renderer to represent the joint kernel.','Temporal marginal kernel ↔ timeWeight']],
 'Moving the center but dropping temporal weight makes a primitive retain its peak indefinitely when it should fade over time.',
 'How does this temporal kernel correspond to STG-Lite width ρ?','Matching the temporal curve alone gives ρ=√(2c). That does not make the complete representations equivalent.',
],
'conditional-example': [
 'Substitute numbers, entry by entry','Work through conditioning in a hand-checkable 4×4 example.',
 [
  ['Compute the outer product, not a dot product','b is a 3D column, so bbᵀ is 3×3. Only its top-left entry is 1.'],
  ['Subtract to get conditional covariance','The first variance of A drops from 2 to 1; the others stay 1. At a fixed time, the spatial kernel is therefore a unit sphere.'],
  ['Move the conditional center with time','With zero 4D mean and b/c=(1,0,0), the center moves along x by one world unit per model-time unit.'],
  ['Verify directly with the inverse matrix','The inverse of the x,t block [[2,1],[1,1]] is [[1,−1],[−1,2]]. Completing the quadratic square gives (x−t)²+t². Add y² and z².'],
 ],
 'At t=2 and (x,y,z)=(2,0,0), the spatial point is at its conditional center and the temporal offset is 2. Both calculations agree.',
 [['Call the downloadable teaching module with this A,b,c. Numerical checks compare the full 4D quadratic form with its factored kernels.','A=diag(2,1,1); b=[1,0,0]; c=1']],
 'This example has a linear trajectory and fixed conditional shape. It does not show that one joint 4D Gaussian can express an arbitrary STG cubic trajectory.',
 'Which axis becomes too wide if you incorrectly use A as conditional covariance?','The x axis: its standard deviation incorrectly becomes √2 instead of 1.',
],
'spherical-harmonics': [
 'Define basis functions → build a finite expansion','SH color is a sum of directional patterns, each multiplied by a coefficient.',
 [
  ['Normalize the viewing direction','Color may vary with viewing direction v. SH selects standard spherical patterns Y; RGB coefficients control each pattern’s contribution. A finite-order expansion is an approximate representation.'],
  ['Derive the constant DC basis value','To give a constant pattern unit squared integral over the sphere, its value k must satisfy 4πk²=1. Thus Y00=1/√(4π).'],
  ['Build first-order patterns from x,y,z','The sphere treats all three axes symmetrically and x²+y²+z²=1, so each squared coordinate integrates to 4π/3. Multiplying by √(3/4π) gives unit squared integral. Graphdeco’s real first-order convention uses the order −y,z,−x.'],
  ['Higher orders also have an explicit construction','Use the Legendre polynomial definition: ℓ! multiplies integers 1 through ℓ, with 0!=1; d^ℓ means differentiating ℓ times. Substituting ℓ=0,1,2 yields the three polynomials below.'],
  ['Combine latitude and longitude patterns','For m≥0, first form an associated polynomial, then multiply by sin(mφ) or cos(mφ) and a normalization constant. We explicitly include the (−1)^m convention to avoid sign mismatches between implementations.'],
  ['Why can coefficients be recovered individually?','With these normalizations, basis functions are orthogonal: matching products integrate to 1 and distinct products to 0. Multiply an expansion by Ykn and integrate to isolate its coefficient. GS usually optimizes coefficients directly, with no runtime integration. NIST DLMF gives the higher-order orthogonality theory; runtime only evaluates polynomials.'],
  ['Check whether the actual asset contains SH','This project’s STG-Lite f_dc_0..2 fields directly store RGB. Static Graphdeco 3DGS typically converts DC coefficients with C0*a00+0.5. A shared field name does not imply a shared format.'],
 ],
 'To represent a constant red-channel value 0.8 using static 3DGS DC, store about 1.06347. Current Lite instead stores 0.8 directly.',
 [['The teaching module evaluates through first order: C0*a0 + C1*(−y*a1+z*a2−x*a3). It is a standalone SH example, not part of Lite rendering.','Four terms with ℓ≤1 ↔ coefficients[0..3]'],
  ['The actual shader reads r4.yzw and clamps it; it does not evaluate SH.','Current RGB ↔ splat.r4.yzw']],
 'SH describes view-dependent color, not automatically a relightable material. Reversing directions or changing basis order/signs changes odd-order contributions.',
 'How many RGB coefficient vectors are needed through order L=3?','Each order has 2ℓ+1: 1+3+5+7=16, or (L+1)².',
],
'feature-splatting': [
 'Generalize color composition to features','Why do some STG methods splat features and decode color afterward?',
 [
  ['Replace three color numbers with D feature numbers','Geometry and time still determine alpha, and prefix transmittance is unchanged. Replace each RGB column with a longer feature column.'],
  ['Apply the same weighted sum to every channel','Stack the channels to obtain F=∑Tiαifi. A nonzero background feature must also be multiplied by remaining transmittance and added. This conceptual formula assumes zero background features.'],
  ['Decode features and direction into color','An illustrative network concatenates F and view direction, applies a matrix and bias, then a nonlinearity, and outputs three channels. Match the actual STG feature groups and decoder inputs to its paper and checkpoint.'],
  ['Explain why the order cannot generally be swapped','A nonlinear function usually does not satisfy D(a+b)=D(a)+D(b). Decoding each Gaussian before color composition may therefore differ from composing features before decoding.'],
 ],
 'For near-to-far features (1,2) and (3,0), both alphas 0.5, and zero background features:',
 [['The teaching function composes arbitrary D-dimensional features using the same transparency rule. Any hand-chosen example decoder weights are exercises, not trained STG weights.','F ↔ compositeFeatures return value'],
  ['Lite directly composes three RGB channels. There is no feature render target or MLP call.','D=3 with direct color ↔ current color_opacity.rgb']],
 'Loading full STG requires matching feature channels, temporal encoding, decoder weights, and inputs. Keeping only three f_dc fields is insufficient.',
 'Can composition and decoding always be swapped if the decoder is linear?','For a purely linear decoder and consistent background treatment, yes. With a bias, the sum of weights also matters; omitting background can invalidate the swap.',
],
};
