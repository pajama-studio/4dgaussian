import { lesson as L, step as S, codeLink as C } from './lesson-format.mjs';

export const trainingLessons = {
  objective: L('误差度量的选择', '渲染得到图像之后，训练到底在减少哪个数字？', ['statistics','derivatives','chain'], [
    S('把预测和目标逐通道相减', '把所有参与训练的像素通道排成 M 个数。L1 取误差绝对值再平均；MSE 先平方再平均。二者都非负，完全一致时为 0，但它们不是同一个损失。', "e_j=\\widehat I_j-I_j,\\qquad L_1=\\frac1M\\sum_j|e_j|,\\qquad L_{\\rm MSE}=\\frac1M\\sum_j e_j^2"),
    S('为什么还可能看局部结构', '只比较单个像素会忽略局部亮度和对比度关系。SSIM 先在窗口内求均值、方差与协方差，再构造相似度；下面给出一种常见加权窗口版本，权重 wn 非负且和为 1。', "\\begin{gathered}\\mu_a=\\sum_nw_na_n,\\quad \\sigma_a^2=\\sum_nw_n(a_n-\\mu_a)^2,\\quad \\sigma_{ab}=\\sum_nw_n(a_n-\\mu_a)(b_n-\\mu_b)\\\\\\operatorname{SSIM}(a,b)=\\frac{(2\\mu_a\\mu_b+C_1)(2\\sigma_{ab}+C_2)}{(\\mu_a^2+\\mu_b^2+C_1)(\\sigma_a^2+\\sigma_b^2+C_2)}\\end{gathered}"),
    S('说明 SSIM 是定义，不是假装能唯一推出来', '亮度项利用 2ab=a²+b²−(a−b)²：均值差越小，比例越接近 1。结构项利用两个中心化信号的平方差展开得到类似性质。C1、C2 是防止小分母的正常数；窗口和边界处理由实现决定。', "\\frac{2\\mu_a\\mu_b+C_1}{\\mu_a^2+\\mu_b^2+C_1}=1-\\frac{(\\mu_a-\\mu_b)^2}{\\mu_a^2+\\mu_b^2+C_1}"),
    S('把相似度转成要最小化的损失', '1−SSIM 在相似度最高时最小。λ 是两种误差的权重，R(θ) 是额外正则，例如限制运动大小；不需要时为 0。这是常见训练目标的结构，不是所有 4DGS 的统一公式。', "L=(1-\\lambda)L_1+\\lambda(1-\\operatorname{SSIM})+\\mathcal R(\\theta),\\qquad \\mathcal R=\\gamma\\sum_i\\|\\mathbf m_i\\|^2\\ \\text{(example)}"),
    S('当前 toy trainer 使用更容易核对的 MSE', '平方的导数是 2e，目标图像视为常量；除以 M 得到每个预测通道的起始梯度。L1 在非零误差处则只取符号，零点使用选定的次梯度。', "\\frac{\\partial L_{\\rm MSE}}{\\partial\\widehat I_j}=\\frac{2(\\widehat I_j-I_j)}M,\\qquad \\frac{\\partial L_1}{\\partial\\widehat I_j}=\\frac{\\operatorname{sign}(e_j)}M\\quad(e_j\\ne0)"),
    S('把误差传回去，再更新', 'zero_grad 清除上一轮梯度；backward 按链式法则累计；step 使用优化器修改参数。教学页面的一步演示用 SGD 便于手算，实际 train_toy_stg.py 用 Adam。', "\\theta_{\\rm new}=\\theta-\\eta\\nabla_\\theta L\\qquad\\text{(SGD teaching step)}"),
  ], ['预测 (0.6,0.2,0.1)，目标 (0.5,0.3,0.1)，三通道平均。', "L_{\\rm MSE}=(0.1^2+(-0.1)^2+0)/3=0.0066667,\\quad \\nabla_{\\widehat I}L=(0.0666667,-0.0666667,0)"],
  [C('train-loop','实际代码 square().mean() 是 MSE，没有 SSIM 或运动正则。Adam 的 fitted 列表决定哪些变量会被训练。','L ↔ loss；∇L ↔ .grad；参数更新 ↔ optimizer.step()')],
  '不能把某次 loss 下降直接等同于真实场景重建质量。当前数据是合成图像，旋转和尺度固定，也没有 SfM 与 densification。', '为什么 backward 后画面可能完全没变？', 'backward 只算梯度；尚未 optimizer.step，或者变量不在优化器参数列表里。'),
  'mean-gradient': L('指数链式法则', '把 Gaussian 中心挪向一个像素，alpha 会怎样变？', ['derivatives','chain','matrices'], [
    S('把指数部分命名为 E', '固定 C、o，令 d=p−μ。Q 对称，先按 x、y 分量把 E 展开，避免直接背矩阵求导规则。', "E=-\\frac12(Q_{11}d_x^2+2Q_{12}d_xd_y+Q_{22}d_y^2),\\qquad \\alpha=o\\,e^E"),
    S('对中心 x 分量求导', 'dx 对 μx 的导数是 −1，dy 不变。平方导数的 2 与前面的 1/2 抵消，两个负号又抵消。', "\\frac{\\partial E}{\\partial\\mu_x}=-\\frac12\\left(2Q_{11}d_x(-1)+2Q_{12}d_y(-1)\\right)=Q_{11}d_x+Q_{12}d_y"),
    S('对中心 y 分量同样计算', '合起来正好就是矩阵 Q 乘偏移 d。', "\\frac{\\partial E}{\\partial\\mu_y}=Q_{12}d_x+Q_{22}d_y,\\qquad \\nabla_\\mu E=Qd"),
    S('外层指数再乘一次自己', 'exp 的导数还是 exp，o 视为常量，因此外层导数 o exp(E) 恰好等于 alpha。', "\\nabla_\\mu\\alpha=\\frac{\\partial\\alpha}{\\partial E}\\nabla_\\mu E=\\alpha Qd"),
    S('注意这只是局部偏导', '中心的真实三维位置还会改变投影大小、方向外观和排序。此处锁住 C，下一条完整链式公式再把其他路径加回来。', "\\delta\\alpha\\approx(\\alpha Qd)^T\\delta\\mu"),
  ], ['C=diag(4,1)、d=(2,1)、o=0.8，alpha≈0.294304。', "Qd=(0.5,1)^T,\\quad \\nabla_\\mu\\alpha\\approx(0.147152,0.294304)^T"],
  [C('toy-kernel','delta=pixel−uv、inverse=inv(covariance)、exp(power) 组成这条路径；PyTorch 自动微分负责同样的计算。','μ ↔ uv；d ↔ delta；Q ↔ inverse')],
  '把 d 写成 μ−p 却仍使用正号，会把梯度方向反过来。clamp 或 discard 的分段梯度也不在此式内。', '像素恰好在中心，均值偏导是多少？', '0，因为 d=0；峰值附近向任意方向小挪动的影响从二阶开始。'),
  'cov-gradient': L('逆矩阵微分，拆成代数步骤', '怎样知道把椭圆变宽会让某个像素变亮多少？', ['matrices','derivatives','chain'], [
    S('从逆矩阵的定义出发', 'Q=C⁻¹，所以 CQ=I。让 C 增加很小的 δC，Q 增加 δQ，乘开后减去 CQ=I。', "(C+\\delta C)(Q+\\delta Q)=I\\quad\\Longrightarrow\\quad C\\delta Q+\\delta C Q+\\delta C\\delta Q=0"),
    S('只保留一次小量', 'δCδQ 是二次小量，在导数极限中消失。左乘 Q 得到 δQ=−QδCQ。这是逆矩阵求导的来源。', "C\\delta Q+\\delta C Q\\approx0\\quad\\Longrightarrow\\quad\\delta Q\\approx-Q\\,\\delta C\\,Q"),
    S('把它代入指数的变化', '固定 d，E=−dᵀQd/2。两个负号抵消；令 v=Qd，则结果只剩 vᵀδCv/2。', "\\delta E=-\\frac12d^T\\delta Qd\\approx\\frac12d^TQ\\,\\delta C\\,Qd=\\frac12v^T\\delta C v"),
    S('逐元素读出系数', '把 vᵀδCv 展开为 ∑ij vi vj δCij。每个 δCij 前面的系数就是 E 对那个矩阵元素的偏导；再乘指数导数 alpha。', "\\delta\\alpha\\approx\\frac\\alpha2\\sum_{ij}v_iv_j\\delta C_{ij}\\quad\\Longrightarrow\\quad\\frac{\\partial\\alpha}{\\partial C}=\\frac\\alpha2vv^T=\\frac\\alpha2Qdd^TQ"),
    S('实际只存一个 b 时要加两项', 'C12 和 C21 同时等于 b；b 增加会同时改变两个元素。因此 db 的导数是两项之和，不能只读矩阵右上角。', "C=\\begin{bmatrix}a&b\\\\b&c\\end{bmatrix}:\\quad \\frac{\\partial\\alpha}{\\partial(a,b,c)}=\\left(\\frac\\alpha2v_x^2,\\ \\alpha v_xv_y,\\ \\frac\\alpha2v_y^2\\right)"),
  ], ['继续用 alpha≈0.294304、v=(0.5,1)。', "\\frac{\\partial\\alpha}{\\partial(a,b,c)}\\approx(0.036788,0.147152,0.147152)"],
  [C('toy-kernel','torch.linalg.inv 的反向传播与这条逆矩阵规则一致；WGSL 仅负责前向，没有对应训练 backward。','C ↔ covariance；∂α/∂C ↔ autograd 中间梯度')],
  '该式用于峰值归一的核。若额外含 det(C) 的概率密度归一化因子，其导数还有一项，不能照抄。', '像素位于中心时，只变宽度会改变未归一化核的峰值吗？', '不会。d=0，协方差梯度为 0；它仍等于 o。'),
  'opacity-gradient': L('sigmoid 的导数', '为什么训练 opacity 用 logit，而梯度里出现 1−o？', ['derivatives','chain','exp'], [
    S('把 sigmoid 写成倒数', '令 o=(1+e⁻ᵝ)⁻¹。外层倒数求导给负号，内层 e⁻ᵝ 又给负号，两者抵消。', "\\frac{do}{d\\beta}=-(1+e^{-\\beta})^{-2}(-e^{-\\beta})=\\frac{e^{-\\beta}}{(1+e^{-\\beta})^2}"),
    S('把结果改写成 o 的乘积', '一个分母给 o，剩下的比例等于 1−o。', "1-o=\\frac{e^{-\\beta}}{1+e^{-\\beta}},\\qquad \\frac{do}{d\\beta}=o(1-o)"),
    S('乘上固定的时间核和空间核', '令 K=w(t)G(p)，与 β 无关；alpha=oK。求导得到 K o(1−o)，把 Ko 换回 alpha。', "\\frac{\\partial\\alpha}{\\partial\\beta}=K\\,o(1-o)=\\alpha(1-o)"),
    S('再接像素损失的上游梯度', 'β 改变 alpha，alpha 改变像素，像素改变 loss。每个受影响像素的贡献都要累加。', "\\frac{\\partial L}{\\partial\\beta_i}=\\sum_p\\frac{\\partial L}{\\partial\\alpha_i(p)}\\,\\alpha_i(p)(1-o_i)"),
  ], ['β=0，o=0.5，K=0.6，所以 alpha=0.3。', "\\frac{\\partial\\alpha}{\\partial\\beta}=0.3(1-0.5)=0.15"],
  [C('toy-kernel','opacity_logits 参与 torch.sigmoid；导出时写回原始 logit，GPU 再解码。','β ↔ opacity_logits；o ↔ torch.sigmoid(...)')],
  '这里 1−o 用基础 sigmoid opacity，不是 1−alpha，也不是 1−时间衰减后的 opacity。', '为什么 β 非常大时难以继续训练 opacity？', 'sigmoid 接近 1，o(1−o) 接近 0，梯度饱和。'),
  'full-chain': L('多条路径的链式法则', '训练位置时，为什么只乘 Jᵀ 会漏梯度？', ['chain','matrices'], [
    S('先画出同一位置的两条出口', '三维中心同时决定屏幕均值 μ2D=π(μc) 和 Jacobian J(μc)；后者又决定 C。暂时固定颜色、opacity 和世界协方差。', "\\mu_c\\to\\mu_{2D}\\to\\alpha\\to L,\\qquad \\mu_c\\to J\\to C\\to\\alpha\\to L"),
    S('先对一个三维坐标分量展开', '比如 x：loss 随两个均值分量和四个协方差元素变化；每条路径都是“上游梯度×局部斜率”，最后相加。若只存 a,b,c，则按三个独立量写等价求和。', "\\frac{\\partial L}{\\partial x}=\\sum_{j=1}^2\\frac{\\partial L}{\\partial\\mu_{2D,j}}\\frac{\\partial\\mu_{2D,j}}{\\partial x}+\\sum_{a,b=1}^2\\frac{\\partial L}{\\partial C_{ab}}\\frac{\\partial C_{ab}}{\\partial x}"),
    S('把 x,y,z 三条式子排成列', 'vec 只是按固定顺序把矩阵元素排成向量；一旦两边使用相同顺序，就能把逐项求和写成矩阵乘法。转置负责把输出梯度送回输入。', "\\nabla_{\\mu_c}L=J^T\\nabla_{\\mu_{2D}}L+\\left(\\frac{\\partial\\operatorname{vec}C}{\\partial\\mu_c}\\right)^T\\nabla_{\\operatorname{vec}C}L"),
    S('C 的变化本身也有左右两条路', 'C=JΣcJᵀ+εI；δJ 可以出现在左边，也可以出现在右边。Σc 暂时固定，ε 是常数。', "\\delta C=(\\delta J)\\Sigma_cJ^T+J\\Sigma_c(\\delta J)^T"),
    S('直接给一个均值路径为零的例子', '球位于光轴上，x=y=0，均匀世界尺度 s。前后移动不会改变屏幕中心，但投影方差 f²s²/z² 会改变。因此只保留第一项会把深度梯度误算成 0。', "C=(f^2s^2/z^2+\\varepsilon)I_2,\\qquad \\frac{\\partial C}{\\partial z}=-\\frac{2f^2s^2}{z^3}I_2"),
    S('其余可训练参数也沿自己的边连接', '回到世界位置需左乘 Rcwᵀ。若训练 log-scale，Σ=∑akakᵀ 且 ak=e^ℓk rk，对 ℓk 求导得到 2akakᵀ；若训练旋转，还需接四元数矩阵及 normalize 的导数。', "\\nabla_{\\mu_w}L=R_{cw}^T\\nabla_{\\mu_c}L,\\qquad \\frac{\\partial\\Sigma}{\\partial\\ell_k}=2a_ka_k^T"),
  ], ['f=100、s=0.1、z=10，投影方差=1.3；深度变化率为 −0.2。', "C=1.3I_2,\\qquad \\partial C/\\partial z=-0.2I_2"],
  [C('toy-project','xyz 同时进入 uv 和 jacobian，这就是两条计算图路径。不要 detach jacobian 后仍声称训练的是完整位置梯度。','μc ↔ xyz；μ2D ↔ uv；C ↔ covariance')],
  '实际若颜色依赖观察方向，还要补颜色路径；若 opacity、旋转依赖时间，也要补对应时间路径。排序和硬可见性变化是分段边界。', '在哪段代码最容易漏掉第二条路径？', '预先缓存或 detach J、C 的训练实现；当前 toy 的 J 由可导 xyz 重新计算。'),
  'composite-gradient': L('分离前缀、本层和尾部', '让前景更不透明，也会挡住后景；梯度怎样同时记住两件事？', ['chain','vectors'], [
    S('固定顺序，只观察第 i 层', 'Ai 表示前面各层已经贡献的颜色；Ti 是到达本层前的透射率；Bi 是从本层之后开始独立合成的尾部颜色，包含背景。', "\\mathbf C=\\mathbf A_i+T_i\\left[\\alpha_i\\mathbf c_i+(1-\\alpha_i)\\mathbf B_i\\right]"),
    S('锁住其他层，对 alpha 求导', 'Ai、Ti、ci、Bi 都视为常量。第一项贡献 ci，第二项贡献 −Bi。', "\\frac{\\partial\\mathbf C}{\\partial\\alpha_i}=T_i(\\mathbf c_i-\\mathbf B_i)"),
    S('锁住 alpha，对当前颜色求导', 'RGB 各通道独立相乘，当前颜色的每一通道只改变输出的同一通道，因此 Jacobian 是系数乘 I3。', "\\frac{\\partial\\mathbf C}{\\partial\\mathbf c_i}=T_i\\alpha_iI_3"),
    S('接上标量 loss', '像素 loss 的 RGB 梯度是三维列；对 alpha 的梯度是它与颜色差做点积。这样颜色差的正负才正确参与参数更新。', "\\frac{\\partial L}{\\partial\\alpha_i}=(\\nabla_{\\mathbf C}L)^TT_i(\\mathbf c_i-\\mathbf B_i),\\qquad \\nabla_{\\mathbf c_i}L=T_i\\alpha_i\\nabla_{\\mathbf C}L"),
  ], ['前景红、后景蓝 alpha=0.5、黑背景；第一层 T1=1，B1=(0,0,0.5)。', "\\frac{\\partial\\mathbf C}{\\partial\\alpha_1}=(1,0,-0.5)^T"],
  [C('toy-composite','autograd 穿过 alpha 的直接颜色贡献和 cumprod 的遮挡贡献；手写 backward 时两者都要保留。','Ai/Bi ↔ 合成链前缀/后缀；Ti ↔ transmittance')],
  'Bi 是未乘 Ti 的尾部合成颜色。若先乘了前缀再代公式，就把 Ti 乘了两次。', '前景颜色和后方合成颜色完全一样时，alpha 的颜色梯度是多少？', '0；增加遮挡不会改变这一个像素的颜色。'),
  'time-gradient': L('时间链的逐项求导', '时间、宽度、运动系数分别怎样接收梯度？', ['derivatives','chain','exp'], [
    S('给时间核的指数命名', 'E=−Δt²/ρ²；固定 τ、ρ 时，Δt 对 t 的导数是 1。先求 E 的导数，再乘外层 w。', "\\frac{dE}{dt}=-\\frac{2\\Delta t}{\\rho^2},\\qquad \\frac{dw}{dt}=w\\frac{dE}{dt}=-\\frac{2\\Delta t}{\\rho^2}w"),
    S('对 log-width 求导，不是直接对宽度求导', '把 ρ=e^ℓτ 代入，E=−Δt²e^(−2ℓτ)。指数求导带出 −2，与前面负号抵消。', "\\frac{\\partial E}{\\partial\\ell_\\tau}=2\\Delta t^2e^{-2\\ell_\\tau}=\\frac{2\\Delta t^2}{\\rho^2},\\qquad \\frac{\\partial w}{\\partial\\ell_\\tau}=\\frac{2\\Delta t^2}{\\rho^2}w"),
    S('时间中心的符号相反', 'Δt=t−τ，对 τ 的偏导为 −1；所以只看时间核这条路，τ 的导数与 t 相反。若 τ 同时进入运动，还必须累加运动路径。', "\\frac{\\partial w}{\\partial\\tau}=\\frac{2\\Delta t}{\\rho^2}w"),
    S('运动系数只出现在一次乘法里', '第 k 阶系数每个分量乘同一个 Δt^k；只影响同一空间分量，因此 Jacobian 是这个标量乘 I3。', "\\frac{\\partial\\mu}{\\partial\\mathbf m_k}=\\Delta t^kI_3,\\qquad \\nabla_{\\mathbf m_k}L=\\Delta t^k\\nabla_\\mu L"),
    S('时间变化同时走多条路', '一条改 opacity，一条改位置，还有旋转和可能的特征时间分支。数学导数存在不代表实现一定传递；detach 会有意切断某条边。', "\\frac{dL}{dt}=\\frac{\\partial L}{\\partial w}\\frac{dw}{dt}+(\\nabla_\\mu L)^T\\frac{d\\mu}{dt}+(\\nabla_q L)^T\\frac{dq}{dt}+\\cdots"),
  ], ['Δt=ρ=0.25 时 w=e⁻¹。', "\\frac{dw}{dt}=-8e^{-1}\\approx-2.943036,\\qquad \\frac{\\partial w}{\\partial\\ell_\\tau}=2e^{-1}\\approx0.735759"],
  [C('motion','GPU 前向分别计算 dt、center、quaternion、temporal_opacity；当前 toy 只训练线性 motion，没有训练 τ、ρ 或 ω。','τ ↔ r0.w；ℓτ ↔ r1.x；mk ↔ motion 字段')],
  '时间宽度达到 max(exp(log-width),1e−6) 的保护区后，关于 log-width 的实际分支导数不再是未裁剪公式。', 'Δt=0 时，一次 motion 参数从这一时刻的图像得到什么位置梯度？', '0，因为 ∂μ/∂m1=Δt I=0；需要其他时刻来约束运动。'),
  'finite-difference': L('用两次前向验证反向', '怎样不用相信手推或 autograd，就独立检查一个梯度？', ['derivatives','chain'], [
    S('一次只改一个参数', 'ek 表示只有第 k 项为 1 的向量。固定图像、相机、时间、其他参数，分别把 θk 增加 h 和减少 h。', "L_+=L(\\theta+h e_k),\\qquad L_-=L(\\theta-h e_k)"),
    S('用中间对称的两点求斜率', '两次参数相差 2h，所以分母是 2h。它近似中心处的变化率。', "g_{\\rm FD}=\\frac{L_+-L_-}{2h}\\approx\\frac{\\partial L}{\\partial\\theta_k}"),
    S('解释为什么对称差分更准确', '平滑函数局部展开的二次项在相减时抵消；除以 2h 后剩下的主要误差是 h² 级别。O(h²) 表示误差随 h² 缩小的量级，不是精确常数。', "L(\\theta\\pm h)=L(\\theta)\\pm hL^{\\prime}+\\frac{h^2}{2}L^{\\prime\\prime}\\pm\\frac{h^3}{6}L^{\\prime\\prime\\prime}+\\cdots\\quad\\Rightarrow\\quad g_{\\rm FD}=L^{\\prime}+O(h^2)"),
    S('不要把 h 越小越好当口诀', '过大的 h 有截断误差，过小的 h 会把相近浮点数相减，舍入误差被放大。用 float64，比较 1e−4、1e−5、1e−6 多组，而不是只挑一个看起来好的结果。', "E_{\\rm abs}=|g_{\\rm analytic}-g_{\\rm FD}|,\\qquad E_{\\rm scaled}=\\frac{E_{\\rm abs}}{\\max(1,|g_{\\rm analytic}|,|g_{\\rm FD}|)}"),
    S('确保两次前向处在同一个光滑分支', '深度换序、clip/cutoff 边界、alpha 上限、零范数会使局部光滑假设失效。不要通过放宽容差隐藏这类变化；先选稳定例子，再单独测试分支。', "L(\\theta)=(\\theta-3)^2,\\quad\\theta=1:\\quad g_{\\rm FD}=\\frac{(-2+h)^2-(-2-h)^2}{2h}=-4"),
  ], ['把解析梯度用于一次小更新后，再跑前向。下降只能作为额外检查，不能替代逐参数有限差分。', "\\theta_{\\rm new}=1-0.1(-4)=1.4,\\qquad L:4\\to2.56"],
  [C('finite-check','现有数值检查分别验证均值、协方差独立项、logit、时间和归一化导数。新增像素实验还检查包含 C 路径的完整位置梯度。','gFD ↔ finiteJacobian；解析值 ↔ expected')],
  '单个标量通过检查不等于完整 CUDA/WGSL backward 正确。本页检查数学与教学代码，当前 WGSL 没有训练 backward。', 'analytic=0、finite difference≈0 时只算相对误差有什么问题？', '分母接近 0，比例会失真；需要绝对误差与带尺度下限的误差一起看。'),
};
